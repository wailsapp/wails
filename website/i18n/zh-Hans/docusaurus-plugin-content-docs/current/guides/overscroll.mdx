
# Overscroll

[Overscroll](https://developer.mozilla.org/en-US/docs/Web/CSS/overscroll-behavior) is the "bounce effect" you sometimes get when you scroll beyond a page's content boundaries. 这在移动应用中是常见的。 可以使用 CSS 禁用此功能：

```css
body v.
    overscroding behavior: none;
}
```