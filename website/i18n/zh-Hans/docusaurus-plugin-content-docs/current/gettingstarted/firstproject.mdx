---
sidebar_position: 2
---

# 创建一个项目

## 项目生成

现在安装了CLI，你可以通过使用 `wils 嵌入` 命令来生成一个新项目。

选择您最喜欢的框架：

从 "../../src/components/framework"导入TabsFrameworks;
<TabsFrameworks/>

<hr />

还有 [个社区模板](../community/templates.mdx) 可用来提供不同的能力和框架。

要看看其他可用的选项，您可以运行 `个wils init -help`。 更多详情可在 [CLI 引用](../reference/cli.mdx#init) 中找到。

## 项目布局

Wail项目有以下布局：

```
.
├── build/
│   ├── appicon.png
│   ├── darwin/
│   └── windows/
├── frontend/
├── go.mod
├── go.sum
├── main.go
└── wails.json
```

### 项目结构已经断开

- `/main.go` - 主要应用程序
- `/frontend/` - 前端项目文件
- `/build/` - 项目构建目录
- `/build/appicon.png` - 应用程序图标
- `/build/darwin/` - Mac 特定项目文件
- `/build/windows/` - Windows 特定项目文件
- `/wails.json` - 项目配置
- `/go.mod` - Go module file
- `/go.sum` - Go module checksum file

`前端` 目录没有特定的邮件地址，可以是您选择的任何前端项目。

`构建` 目录在构建过程中被使用。 这些文件可能被更新以自定义您的构建。 如果将 文件从构建目录中删除，默认版本将会重新生成。

`go.mod` 中的默认模块名称为“changeme”。 您应该将此更改为更合适的东西。
