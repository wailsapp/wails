---
sidebar_position: 1
---

# 安装

## 支持的平台

- Windows 10/11 AMD64/ARM64
- MacOS 10.13+ AMD64
- MacOS 11.0+ ARM64
- Linux AMD64/ARM64

## 依赖关系

安装前需要的一些常用依赖：

- 去1.17+
- NPM (节点 15+)

### 转至

从 [Go Downloads 页面](https://go.dev/doc/install) 下载。

请确保您遵循官方的 [去安装说明](https://go.dev/doc/install)。 您还需要确保您的 `PATH` 环境变量也包括您 `~/go/bin` 目录的路径。 重新启动您的终端并进行以下检查：

- 检查操作已正确安装： `转到版本`
- 检查"~/go/bin"在您的 PATH 变量中： `echo $PATH | grep go/bin`

### NPM

从 [节点下载页面](https://nodejs.org/en/download/) 下载NPM 。 最好是利用最新版本，因为这是我们通常所检验的。

运行 `npm --version` 进行验证。

## 平台特定依赖关系

您还需要安装平台特定依赖：

从 "../../src/components/tabinstall"导入TabsInstaller；
<TabsInstaller />

## 可选依赖关系

- [UPX](https://upx.github.io/) 用于压缩您的应用程序。

## 正在安装路径

运行 `去安装 github.com/wailsapp/wails/v2/cmd/wails@latest` 来安装Wails CLI。

## 系统检查

运行 `wails doctor` 将检查您是否安装了正确的依赖关系。 如果没有，它将就所缺少的东西提出建议，并帮助如何纠正任何问题。

## `wails` 命令似乎丢失了？

If your system is reporting that the `wails` command is missing, make sure you have followed the Go installation guide correctly. 通常情况下，这意味着用户主目录中的 `go/bin` 目录不在 `PATH` 环境 变量。 您通常还需要关闭并重新打开任何打开的命令提示，以便安装器对环境 的更改能够反映在命令提示中。