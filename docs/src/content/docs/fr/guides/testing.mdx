---
title: Tests
description: Testez votre application Wails
sidebar:
  order: 5
---

## Vue d'ensemble

Les tests garantissent que votre application fonctionne correctement et empêchent les régressions.

## Tests unitaires

### Tester les services

```go
func TestUserService_Create(t *testing.T) {
    service := &UserService{
        users: make(map[string]*User),
    }
    
    user, err := service.Create("john@example.com", "password123")
    if err != nil {
        t.Fatalf("unexpected error: %v", err)
    }
    
    if user.Email != "john@example.com" {
        t.Errorf("expected email john@example.com, got %s", user.Email)
    }
}
```

### Tester avec des mocks

```go
type MockDB struct {
    users map[string]*User
}

func (m *MockDB) Create(user *User) error {
    m.users[user.ID] = user
    return nil
}

func TestUserService_WithMock(t *testing.T) {
    mockDB := &MockDB{users: make(map[string]*User)}
    service := &UserService{db: mockDB}
    
    user, err := service.Create("test@example.com", "pass")
    if err != nil {
        t.Fatal(err)
    }
    
    if len(mockDB.users) != 1 {
        t.Error("expected 1 user in mock")
    }
}
```

## Tests d'intégration

### Tester avec de vraies dépendances

```go
func TestIntegration(t *testing.T) {
    // Setup test database
    db, err := sql.Open("sqlite3", ":memory:")
    if err != nil {
        t.Fatal(err)
    }
    defer db.Close()
    
    // Create schema
    _, err = db.Exec(`CREATE TABLE users (...)`)
    if err != nil {
        t.Fatal(err)
    }
    
    // Test service
    service := &UserService{db: db}
    user, err := service.Create("test@example.com", "password")
    if err != nil {
        t.Fatal(err)
    }
    
    // Verify in database
    var count int
    db.QueryRow("SELECT COUNT(*) FROM users").Scan(&count)
    if count != 1 {
        t.Errorf("expected 1 user, got %d", count)
    }
}
```

## Tests frontend

### Tests unitaires JavaScript

```javascript
// Using Vitest
import { describe, it, expect } from 'vitest'
import { formatDate } from './utils'

describe('formatDate', () => {
    it('formats date correctly', () => {
        const date = new Date('2024-01-01')
        expect(formatDate(date)).toBe('2024-01-01')
    })
})
```

### Tester les liaisons

```javascript
import { vi } from 'vitest'
import { GetUser } from './bindings/myapp/userservice'

// Mock the binding
vi.mock('./bindings/myapp/userservice', () => ({
    GetUser: vi.fn()
}))

describe('User Component', () => {
    it('loads user data', async () => {
        GetUser.mockResolvedValue({ name: 'John', email: 'john@example.com' })
        
        // Test your component
        const user = await GetUser(1)
        expect(user.name).toBe('John')
    })
})
```

## Bonnes pratiques

### ✅ À faire

- Écrivez les tests avant de corriger les bugs
- Testez les cas limites
- Utilisez des tests pilotés par des tables (table-driven tests)
- Mockez les dépendances externes
- Testez la gestion des erreurs
- Gardez les tests rapides

### ❌ À ne pas faire

- Ne sautez pas les cas d'erreur
- Ne testez pas les détails d'implémentation
- N'écrivez pas de tests instables (flaky tests)
- N'ignorez pas les échecs de tests
- Ne sautez pas les tests d'intégration

## Exécution des tests

```bash
# Run Go tests
go test ./...

# Run with coverage
go test -cover ./...

# Run specific test
go test -run TestUserService

# Run frontend tests
cd frontend && npm test

# Run with watch mode
cd frontend && npm test -- --watch
```

## Prochaines étapes

- [Tests de bout en bout](/fr/guides/e2e-testing) - Testez les flux utilisateur complets
- [Bonnes pratiques](/fr/features/bindings/best-practices) - Apprenez les bonnes pratiques