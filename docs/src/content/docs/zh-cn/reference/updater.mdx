---
title: Updater API
description: Wails v3 Updater API 的参考文档
sidebar:
  order: 8
---

import { Tabs, TabItem } from '@astrojs/starlight/components';

# Updater API 参考

Updater API 为 Wails 应用程序提供自动更新功能，包括更新检查、下载和安装。

## Go API

### CreateUpdaterService

创建一个新的更新服务。

```go
func CreateUpdaterService(currentVersion string, opts ...UpdaterOption) (*UpdaterService, error)
```

**参数：**
- `currentVersion` - 应用程序的当前版本（例如，"1.0.0"）
- `opts` - 配置选项

**返回：**
- `*UpdaterService` - 更新服务实例
- `error` - 如果创建失败则返回错误

**示例：**
```go
updater, err := application.CreateUpdaterService(
    "1.0.0",
    application.WithUpdateURL("https://updates.example.com/myapp/"),
)
```

### UpdaterOption 函数

#### WithUpdateURL

设置更新清单的基础 URL。

```go
func WithUpdateURL(url string) UpdaterOption
```

**必需。** URL 应指向包含 `update.json` 清单的目录。

#### WithCheckInterval

设置自动后台更新检查的间隔。

```go
func WithCheckInterval(interval time.Duration) UpdaterOption
```

设置为 `0` 以禁用自动检查。默认值为 `0`（已禁用）。

#### WithAllowPrerelease

允许将预发布版本视为更新。

```go
func WithAllowPrerelease(allow bool) UpdaterOption
```

默认值为 `false`。

#### WithChannel

设置更新通道（例如，"stable"、"beta"、"canary"）。

```go
func WithChannel(channel string) UpdaterOption
```

通道将附加到更新 URL 路径中。

#### WithPublicKey

设置用于签名验证的公钥。

```go
func WithPublicKey(key string) UpdaterOption
```

密钥应为 base64 编码的 Ed25519 公钥。

#### WithRequireSignature

要求所有更新都必须经过签名。

```go
func WithRequireSignature(require bool) UpdaterOption
```

默认值为 `false`。

### UpdaterService 方法

#### GetCurrentVersion

返回当前应用程序版本。

```go
func (u *UpdaterService) GetCurrentVersion() string
```

#### CheckForUpdate

检查是否有新版本可用。

```go
func (u *UpdaterService) CheckForUpdate() (*UpdateInfo, error)
```

**返回：**
- `*UpdateInfo` - 更新信息，如果没有可用更新则返回 `nil`
- `error` - 如果检查失败则返回错误

#### DownloadUpdate

下载可用的更新。

```go
func (u *UpdaterService) DownloadUpdate() error
```

在下载期间发出 `updater:progress` 事件。

#### ApplyUpdate

应用已下载的更新。

```go
func (u *UpdaterService) ApplyUpdate() error
```

这将重启应用程序。

#### DownloadAndApply

在一次调用中下载并应用更新。

```go
func (u *UpdaterService) DownloadAndApply() error
```

#### GetState

返回当前更新器状态。

```go
func (u *UpdaterService) GetState() string
```

**可能的值：**
- `"idle"` - 无更新进行中
- `"checking"` - 正在检查更新
- `"available"` - 有可用更新
- `"downloading"` - 正在下载更新
- `"ready"` - 更新已下载，准备安装
- `"installing"` - 正在安装更新
- `"error"` - 发生错误

#### GetUpdateInfo

返回有关可用更新的信息。

```go
func (u *UpdaterService) GetUpdateInfo() *UpdateInfo
```

#### GetLastError

返回最后一个错误消息。

```go
func (u *UpdaterService) GetLastError() string
```

#### Reset

重置更新器状态。

```go
func (u *UpdaterService) Reset()
```

### UpdateInfo

有关可用更新的信息。

```go
type UpdateInfo struct {
    Version      string    `json:"version"`
    ReleaseDate  time.Time `json:"releaseDate"`
    ReleaseNotes string    `json:"releaseNotes"`
    Size         int64     `json:"size"`
    PatchSize    int64     `json:"patchSize,omitempty"`
    Mandatory    bool      `json:"mandatory"`
    HasPatch     bool      `json:"hasPatch"`
}
```

| 字段 | 类型 | 描述 |
|-------|------|-------------|
| `Version` | `string` | 新版本号 |
| `ReleaseDate` | `time.Time` | 该版本的发布日期 |
| `ReleaseNotes` | `string` | 发行说明（markdown 格式） |
| `Size` | `int64` | 完整下载大小（字节） |
| `PatchSize` | `int64` | 补丁大小（字节，如果可用） |
| `Mandatory` | `bool` | 是否必须安装此更新 |
| `HasPatch` | `bool` | 是否有可用补丁 |

### UpdaterConfig

更新器的配置。

```go
type UpdaterConfig struct {
    UpdateURL        string
    CheckInterval    time.Duration
    AllowPrerelease  bool
    PublicKey        string
    RequireSignature bool
    Channel          string
}
```

## JavaScript/TypeScript API

当作为服务添加时，更新服务会自动绑定到前端。

### 类型

```typescript
interface UpdateInfo {
  version: string;
  releaseDate: string;  // ISO 8601 日期字符串
  releaseNotes: string;
  size: number;
  patchSize?: number;
  mandatory: boolean;
  hasPatch: boolean;
}

interface DownloadProgress {
  downloaded: number;
  total: number;
  percentage: number;
  bytesPerSecond: number;
}
```

### 方法

#### GetCurrentVersion

```typescript
function GetCurrentVersion(): string
```

返回当前应用程序版本。

#### CheckForUpdate

```typescript
function CheckForUpdate(): Promise<UpdateInfo | null>
```

检查是否有可用更新。

**返回：** 如果有可用更新则返回 `UpdateInfo`，否则返回 `null`。

#### DownloadUpdate

```typescript
function DownloadUpdate(): Promise<void>
```

下载可用的更新。发出 `updater:progress` 事件。

#### ApplyUpdate

```typescript
function ApplyUpdate(): Promise<void>
```

应用已下载的更新。应用程序将重启。

#### DownloadAndApply

```typescript
function DownloadAndApply(): Promise<void>
```

在一次调用中下载并应用更新。

#### GetState

```typescript
function GetState(): string
```

返回当前更新器状态。

#### GetUpdateInfo

```typescript
function GetUpdateInfo(): UpdateInfo | null
```

返回当前更新信息。

#### GetLastError

```typescript
function GetLastError(): string
```

返回最后一个错误消息。

#### Reset

```typescript
function Reset(): void
```

重置更新器状态。

### 事件

#### updater:progress

在下载期间发出，包含进度信息。

```typescript
import { Events } from '@wailsio/runtime';

Events.On('updater:progress', (data: DownloadProgress) => {
  console.log(`${data.percentage}% downloaded`);
});
```

## 更新清单格式

更新清单 (`update.json`) 必须托管在您的更新 URL 中。

### 模式

```typescript
interface Manifest {
  version: string;
  release_date: string;      // ISO 8601
  release_notes?: string;
  platforms: {
    [platform: string]: PlatformUpdate;
  };
  minimum_version?: string;
  mandatory?: boolean;
}

interface PlatformUpdate {
  url: string;
  size: number;
  checksum: string;          // "sha256:..."
  signature?: string;
  patches?: PatchInfo[];
}

interface PatchInfo {
  from: string;              // 此补丁适用的起始版本
  url: string;
  size: number;
  checksum: string;
  signature?: string;
}
```

### 平台键

| 平台 | 键 |
|----------|-----|
| macOS (Apple Silicon) | `macos-arm64` |
| macOS (Intel) | `macos-amd64` |
| Windows (64-bit) | `windows-amd64` |
| Linux (64-bit) | `linux-amd64` |
| Linux (ARM64) | `linux-arm64` |

### 示例清单

```json
{
  "version": "1.2.0",
  "release_date": "2025-01-15T00:00:00Z",
  "release_notes": "## Changes\n\n- New feature\n- Bug fixes",
  "platforms": {
    "macos-arm64": {
      "url": "https://example.com/app-1.2.0-macos-arm64.tar.gz",
      "size": 12582912,
      "checksum": "sha256:abc123def456...",
      "patches": [
        {
          "from": "1.1.0",```json
        "url": "https://example.com/patches/1.1.0-to-1.2.0.patch",
        "size": 14336,
        "checksum": "sha256:789xyz..."
      }
    ]
  },
  "windows-amd64": {
    "url": "https://example.com/app-1.2.0-windows.zip",
    "size": 14680064,
    "checksum": "sha256:ghi789jkl012..."
  }
},
"minimum_version": "1.0.0",
"mandatory": false
}
```

## 服务集成

### 注册服务

```go
app := application.New(application.Options{
    Name: "MyApp",
    Services: []application.Service{
        application.NewService(updater),
    },
})
```

### 服务生命周期

更新服务实现了 `Service` 接口：

```go
type Service interface {
    ServiceName() string
    ServiceStartup(ctx context.Context, options ServiceOptions) error
    ServiceShutdown() error
}
```

- **ServiceStartup**：如果已配置，则启动后台更新检查
- **ServiceShutdown**：停止后台检查

### 事件处理程序

在 Go 中注册处理程序：

```go
updater.OnUpdateAvailable(func(info *UpdateInfo) {
    log.Printf("Update available: %s", info.Version)
})

updater.OnDownloadProgress(func(downloaded, total int64, percentage float64) {
    log.Printf("Download: %.1f%%", percentage)
})

updater.OnError(func(err string) {
    log.Printf("Update error: %s", err)
})
```

## 安全注意事项

### HTTPS

始终对更新 URL 使用 HTTPS，以防止中间人攻击。

### 校验和验证

所有下载在安装前都会针对 SHA256 校验和进行验证。

### 签名验证

为生产应用程序启用签名验证：

```go
updater, _ := application.CreateUpdaterService(
    "1.0.0",
    application.WithUpdateURL("https://updates.example.com/myapp/"),
    application.WithRequireSignature(true),
    application.WithPublicKey("MCowBQYDK2VwAyEA..."),
)
```

### 代码签名

确保您的应用程序二进制文件已正确签名：
- macOS：Developer ID Application 证书
- Windows：带时间戳的代码签名证书

有关详细信息，请参阅 [代码签名指南](/zh-cn/guides/build/signing)。