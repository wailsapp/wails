---
title: Fenster-Grundlagen
description: Erstellen und Verwalten von Anwendungsfenstern in Wails
sidebar:
  order: 1
---

import { Tabs, TabItem, Card, CardGrid } from "@astrojs/starlight/components";

## Fenster-Verwaltung

Wails bietet eine **einheitliche Fenster-Verwaltungs-API**, die plattformübergreifend auf allen Plattformen funktioniert. Erstellen Sie Fenster, steuern Sie deren Verhalten und verwalten Sie mehrere Fenster mit voller Kontrolle über Erstellung, Erscheinungsbild, Verhalten und Lebenszyklus.

## Schnellstart

```go
package main

import "github.com/wailsapp/wails/v3/pkg/application"

func main() {
    app := application.New(application.Options{
        Name: "My App",
    })

    // Create a window
    window := app.Window.New()
    
    // Configure it
    window.SetTitle("Hello Wails")
    window.SetSize(800, 600)
    window.Center()
    
    // Show it
    window.Show()

    app.Run()
}
```

**Das war's!** Sie haben ein plattformübergreifendes Fenster.

## Fenster erstellen

### Einfaches Fenster

Die einfachste Möglichkeit, ein Fenster zu erstellen:

```go
window := app.Window.New()
```

**Was Sie erhalten:**
- Standardgröße (800x600)
- Standardtitel (Anwendungsname)
- WebView bereit für Ihr Frontend
- Plattform-natives Erscheinungsbild

### Fenster mit Optionen

Erstellen Sie ein Fenster mit benutzerdefinierter Konfiguration:

```go
window := app.Window.NewWithOptions(application.WebviewWindowOptions{
    Title:  "My Application",
    Width:  1200,
    Height: 800,
    X:      100,   // Position from left
    Y:      100,   // Position from top
    AlwaysOnTop: false,
    Frameless: false,
    Hidden: false,
    MinWidth: 400,
    MinHeight: 300,
    MaxWidth: 1920,
    MaxHeight: 1080,
})
```

**Häufige Optionen:**

| Option | Typ | Beschreibung |
|--------|-- ----|-- ------- ----|
| `Title` | `string` | Fenstertitel |
| `Width` | `int` | Fensterbreite in Pixeln |
| `Height` | `int` | Fensterhöhe in Pixeln |
| `X` | `int` | X-Position (von links) |
| `Y` | `int` | Y-Position (von oben) |
| `AlwaysOnTop` | `bool` | Fenster immer im Vordergrund halten |
| `Frameless` | `bool` | Titelleiste und Ränder entfernen |
| `Hidden` | `bool` | Fenster beim Start verstecken |
| `MinWidth` | `int` | Mindestbreite |
| `MinHeight` | `int` | Mindesthöhe |
| `MaxWidth` | `int` | Maximalbreite |
| `MaxHeight` | `int` | Maximale Höhe |

**Siehe [Fenster-Optionen](/de/features/windows/options) für die vollständige Liste.**

### Benannte Fenster

Geben Sie Fenstern Namen, um sie einfach wiederzufinden:

```go
window := app.Window.NewWithOptions(application.WebviewWindowOptions{
    Name:  "main-window",
    Title: "Main Application",
})

// Later, find it by name
mainWindow := app.GetWindowByName("main-window")
if mainWindow != nil {
    mainWindow.Show()
}
```

**Verwendungsfälle:**
- Mehrere Fenster (Hauptfenster, Einstellungen, Über)
- Auffinden von Fenstern aus verschiedenen Teilen Ihres Codes
- Fenster-Kommunikation

## Fenster steuern

### Anzeigen und Verbergen

```go
// Show window
window.Show()

// Hide window
window.Hide()

// Check if visible
if window.IsVisible() {
    fmt.Println("Window is visible")
}
```

**Verwendungsfälle:**
- Startbildschirme (anzeigen, dann verstecken)
- Einstellungsfenster (verstecken, wenn nicht benötigt)
- Popup-Fenster (bei Bedarf anzeigen)

### Position und Größe

```go
// Set size
window.SetSize(1024, 768)

// Set position
window.SetPosition(100, 100)

// Centre on screen
window.Center()

// Get current size
width, height := window.Size()

// Get current position
x, y := window.Position()
```

**Koordinatensystem:**
- (0, 0) ist die obere linke Ecke des primären Bildschirms
- Positives X geht nach rechts
- Positives Y geht nach unten

### Fensterzustand

```go
// Minimise
window.Minimise()

// Maximise
window.Maximise()

// Fullscreen
window.Fullscreen()

// Restore to normal
window.Restore()

// Check state
if window.IsMinimised() {
    fmt.Println("Window is minimised")
}

if window.IsMaximised() {
    fmt.Println("Window is maximised")
}

if window.IsFullscreen() {
    fmt.Println("Window is fullscreen")
}
```

**Zustandsübergänge:**

```
Normal ←→ Minimiert
Normal ←→ Maximiert
Normal ←→ Vollbild
```

### Titel und Erscheinungsbild

```go
// Set title
window.SetTitle("My Application - Document.txt")

// Set background colour
window.SetBackgroundColour(0, 0, 0, 255) // RGBA

// Set always on top
window.SetAlwaysOnTop(true)

// Set resizable
window.SetResizable(false)
```

### Fenster schließen

```go
// Close window
window.Close()

// Destroy window (force close)
window.Destroy()
```

**Unterschied:**
- `Close()` - Löst Schließen-Ereignis aus, kann abgebrochen werden
- `Destroy()` - Sofortige Zerstörung, kann nicht abgebrochen werden

## Fenster finden

### Nach Name

```go
window := app.GetWindowByName("settings")
if window != nil {
    window.Show()
}
```

### Nach ID

Jedes Fenster hat eine eindeutige ID:

```go
id := window.ID()
fmt.Printf("Window ID: %d\n", id)

// Find by ID
foundWindow := app.GetWindowByID(id)
```

### Aktuelles Fenster

Holen Sie sich das aktuell fokussierte Fenster:

```go
current := app.Window.Current()
if current != nil {
    current.SetTitle("Active Window")
}
```

### Alle Fenster

Holen Sie sich alle Fenster:

```go
windows := app.Window.GetAll()
fmt.Printf("Total windows: %d\n", len(windows))

for _, w := range windows {
    fmt.Printf("Window: %s (ID: %d)\n", w.Name(), w.ID())
}
```

## Fenster-Lebenszyklus

### Erstellung

```go
app.OnWindowCreation(func(window *application.WebviewWindow) {
    fmt.Printf("Window created: %s\n", window.Name())
    
    // Configure new windows
    window.SetMinSize(400, 300)
})
```

### Schließen

```go
window.OnClose(func() bool {
    // Return false to cancel close
    // Return true to allow close
    
    if hasUnsavedChanges() {
        result := showConfirmdialog("Unsaved changes. Close anyway?")
        return result == "yes"
    }
    return true
})
```

**Wichtig:** `OnClose` funktioniert nur für vom Benutzer initiierte Schließungen (Klicken auf die X-Schaltfläche). Es verhindert nicht `window.Destroy()`.

### Zerstörung

```go
window.OnDestroy(func() {
    fmt.Println("Window destroyed")
    // Cleanup resources
})
```

## Mehrere Fenster

### Mehrere Fenster erstellen

```go
// Main window
mainWindow := app.Window.NewWithOptions(application.WebviewWindowOptions{
    Name:   "main",
    Title:  "Main Application",
    Width:  1200,
    Height: 800,
})

// Settings window
settingsWindow := app.Window.NewWithOptions(application.WebviewWindowOptions{
    Name:   "settings",
    Title:  "Settings",
    Width:  600,
    Height: 400,
    Hidden: true,  // Start hidden
})

// Show settings when needed
settingsWindow.Show()
```

### Fenster-Kommunikation

Fenster können über Ereignisse kommunizieren:

```go
// In main window
app.Event.Emit("data-updated", map[string]interface{}{
    "value": 42,
})

// In settings window
app.Event.On("data-updated", func(event *application.WailsEvent) {
    data := event.Data.(map[string]interface{})
    value := data["value"].(int)
    fmt.Printf("Received: %d\n", value)
})
```

**Siehe [Ereignisse](/de/features/events/system) für mehr.**

### Parent-Child-Fenster

```go
// Create child window
childWindow := app.Window.NewWithOptions(application.WebviewWindowOptions{
    Title:  "Child Window",
    Parent: mainWindow,  // Set parent
})
```

**Verhalten:**
- Child schließt sich, wenn Parent schließt
- Child bleibt über Parent (auf einigen Plattformen)
- Child minimiert sich mit Parent (auf einigen Plattformen)

**Plattformunterstützung:**
- **macOS:** Vollständige Unterstützung
- **Windows:** Teilweise Unterstützung
- **Linux:** Variiert je nach Desktop-Umgebung

## Plattformspezifische Funktionen

<Tabs syncKey="platform">
  <TabItem label="Windows" icon="seti:windows">
    **Windows-spezifische Funktionen:**
    
    ```go
    // Flash taskbar button
    window.Flash(true)  // Start flashing
    window.Flash(false) // Stop flashing
    
    // Trigger Windows 11 Snap Assist (Win+Z)
    window.SnapAssist()
    
    // Set window icon
    window.SetIcon(iconBytes)
    ```
    
    **Snap Assist:**
    Zeigt Windows 11 Snap-Layout-Optionen für das Fenster an.
    
    **Taskbar-Blitzen:**
    Nützlich für Benachrichtigungen, wenn das Fenster minimiert ist.
  </TabItem>

  <TabItem label="macOS" icon="apple">
    **macOS-spezifische Funktionen:**

    ```go
    // Transparent title bar---

**Fragen?** Stellen Sie diese im [Discord](https://discord.gg/JDdSxwjhGf) oder sehen Sie sich die [Fenster-Beispiele](https://github.com/wailsapp/wails/tree/master/v3/examples) an.