---
title: パフォーマンス最適化
description: Wailsアプリケーションを最大限のパフォーマンスに最適化する方法
sidebar:
  order: 9
---

## 概要

Wailsアプリケーションの速度、メモリ効率、レスポンス性を最適化します。

## フロントエンドの最適化

### バンドルサイズ

```javascript
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
        },
      },
    },
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
      },
    },
  },
}
```

### コード分割

```javascript
// コンポーネントの遅延読み込み
const Settings = lazy(() => import('./Settings'))

function App() {
  return (
    <Suspense fallback={<Loading />}>
      <Settings />
    </Suspense>
  )
}
```

### アセットの最適化

```javascript
// 画像の最適化
import { defineConfig } from 'vite'
import imagemin from 'vite-plugin-imagemin'

export default defineConfig({
  plugins: [
    imagemin({
      gifsicle: { optimizationLevel: 3 },
      optipng: { optimizationLevel: 7 },
      svgo: { plugins: [{ removeViewBox: false }] },
    }),
  ],
})
```

## バックエンドの最適化

### 効icientなバインディング

```go
// ❌ 悪い例: すべてを返す
func (s *Service) GetAllData() []Data {
    return s.db.FindAll() // データ量が膨大になる可能性がある
}

// ✅ 良い例: ページネーションを使用
func (s *Service) GetData(page, size int) (*PagedData, error) {
    return s.db.FindPaged(page, size)
}
```

### キャッシング

```go
type CachedService struct {
    cache *lru.Cache
    ttl   time.Duration
}

func (s *CachedService) GetData(key string) (interface{}, error) {
    // キャッシュを確認
    if val, ok := s.cache.Get(key); ok {
        return val, nil
    }
    
    // データを取得してキャッシュ
    data, err := s.fetchData(key)
    if err != nil {
        return nil, err
    }
    
    s.cache.Add(key, data)
    return data, nil
}
```

### 長時間の処理にはゴルーチンを使用

```go
func (s *Service) ProcessLargeFile(path string) error {
    // バックグラウンドで処理
    go func() {
        result, err := s.process(path)
        if err != nil {
            s.app.Event.Emit("process-error", err.Error())
            return
        }
        s.app.Event.Emit("process-complete", result)
    }()
    
    return nil
}
```

## メモリ最適化

### メモリリークの回避

```go
// ❌ 悪い例: ゴルーチンのリーク
func (s *Service) StartPolling() {
    ticker := time.NewTicker(1 * time.Second)
    go func() {
        for range ticker.C {
            s.poll()
        }
    }()
    // tickerが停止されない！
}

// ✅ 良い例: 適切なクリーンアップ
func (s *Service) StartPolling() {
    ticker := time.NewTicker(1 * time.Second)
    s.stopChan = make(chan bool)
    
    go func() {
        for {
            select {
            case <-ticker.C:
                s.poll()
            case <-s.stopChan:
                ticker.Stop()
                return
            }
        }
    }()
}

func (s *Service) StopPolling() {
    close(s.stopChan)
}
```

### リソースプールの活用

```go
var bufferPool = sync.Pool{
    New: func() interface{} {
        return new(bytes.Buffer)
    },
}

func processData(data []byte) []byte {
    buf := bufferPool.Get().(*bytes.Buffer)
    defer bufferPool.Put(buf)
    
    buf.Reset()
    buf.Write(data)
    // 処理...
    return buf.Bytes()
}
```

## イベント最適化

### イベントのデバウンス

```javascript
// 頻繁なイベントのデバウンス
let debounceTimer
function handleInput(value) {
    clearTimeout(debounceTimer)
    debounceTimer = setTimeout(() => {
        UpdateData(value)
    }, 300)
}
```

### バッチ更新

```go
type BatchProcessor struct {
    items []Item
    mu    sync.Mutex
    timer *time.Timer
}

func (b *BatchProcessor) Add(item Item) {
    b.mu.Lock()
    defer b.mu.Unlock()
    
    b.items = append(b.items, item)
    
    if b.timer == nil {
        b.timer = time.AfterFunc(100*time.Millisecond, b.flush)
    }
}

func (b *BatchProcessor) flush() {
    b.mu.Lock()
    items := b.items
    b.items = nil
    b.timer = nil
    b.mu.Unlock()
    
    // バッチ処理
    processBatch(items)
}
```

## ビルドの最適化

### バイナリサイズ

```bash
# デバッグシンボルを削除
wails3 build -ldflags "-s -w"

# バイナリサイズをさらに削減
go build -ldflags="-s -w" -trimpath
```

### コンパイル速度

```bash
# ビルドキャッシュを使用
go build -buildmode=default

# 並列コンパイル
go build -p 8
```

## プロファイリング

### CPUプロファイリング

```go
import "runtime/pprof"

func profileCPU() {
    f, _ := os.Create("cpu.prof")
    defer f.Close()
    
    pprof.StartCPUProfile(f)
    defer pprof.StopCPUProfile()
    
    // プロファイリング対象のコード
}
```

### メモリプロファイリング

```go
import "runtime/pprof"

func profileMemory() {
    f, _ := os.Create("mem.prof")
    defer f.Close()
    
    runtime.GC()
    pprof.WriteHeapProfile(f)
}
```

### プロファイルの分析

```bash
# CPUプロファイルを表示
go tool pprof cpu.prof

# メモリプロファイルを表示
go tool pprof mem.prof

# ウェブインターフェース
go tool pprof -http=:8080 cpu.prof
```

## ベストプラクティス

### ✅ 推奨

- 最適化の前にプロファイリングを行う
- 高コストな処理をキャッシュする
- 大量のデータセットにはページネーションを使用する
- 頻繁なイベントをデバウンスする
- リソースプールを活用する
- ゴルーチンを適切にクリーンアップする
- バンドルサイズを最適化する
- 遅延読み込みを使用する

### ❌ 非推奨

- 早期最適化を行わない
- メモリリークを無視しない
- メainsスレッドをブロックしない
- 巨大なデータセットを返さない
- プロファイリングをスキップしない
- クリーンアップを忘れない

## パフォーマンスチェックリスト

- [ ] フロントエンドバンドルの最適化
- [ ] 画像の圧縮
- [ ] コード分割の実装
- [ ] バックエンドメソッドのページネーション
- [ ] キャッシングの実装
- [ ] ゴルーチンのクリーンアップ
- [ ] イベントのデバウンス
- [ ] バイナリサイズの最適化
- [ ] プロファイリングの実施
- [ ] メモリリークの修正

## 次のステップ

- [アーキテクチャ](/ja/guides/architecture) - アプリケーションアーキテクチャのパターン
- [テスト](/ja/guides/testing) - アプリケーションのテスト
- [ビルド](/guides/building) - 最適化されたバイナリのビルド