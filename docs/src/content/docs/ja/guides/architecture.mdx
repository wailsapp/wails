---
title: アーキテクチャパターン
description: Wailsアプリケーションのデザインパターン
sidebar:
  order: 7
---

## 概要

Wailsアプリケーションの構成に役立つ実証済みのパターンです。

## サービスレイヤーパターン

### 構造

```
app/
├── main.go
├── services/
│   ├── user_service.go
│   ├── data_service.go
│   └── file_service.go
└── models/
    └── user.go
```

### 実装

```go
// サービスインターフェース
type UserService interface {
    Create(email, password string) (*User, error)
    GetByID(id int) (*User, error)
    Update(user *User) error
    Delete(id int) error
}

// 実装
type userService struct {
    app *application.Application
    db  *sql.DB
}

func NewUserService(app *application.Application, db *sql.DB) UserService {
    return &userService{app: app, db: db}
}
```

## リポジトリパターン

### 構造

```go
// リポジトリインターフェース
type UserRepository interface {
    Create(user *User) error
    FindByID(id int) (*User, error)
    Update(user *User) error
    Delete(id int) error
}

// サービスはリポジトリを使用する
type UserService struct {
    repo UserRepository
}

func (s *UserService) Create(email, password string) (*User, error) {
    user := &User{Email: email}
    return user, s.repo.Create(user)
}
```

## イベント駆動アーキテクチャ

### イベントバス

```go
type EventBus struct {
    app       *application.Application
    listeners map[string][]func(interface{})
    mu        sync.RWMutex
}

func (eb *EventBus) Subscribe(event string, handler func(interface{})) {
    eb.mu.Lock()
    defer eb.mu.Unlock()
    eb.listeners[event] = append(eb.listeners[event], handler)
}

func (eb *EventBus) Publish(event string, data interface{}) {
    eb.mu.RLock()
    handlers := eb.listeners[event]
    eb.mu.RUnlock()
    
    for _, handler := range handlers {
        go handler(data)
    }
}
```

### 使用例

```go
// サブスクライブ
eventBus.Subscribe("user.created", func(data interface{}) {
    user := data.(*User)
    sendWelcomeEmail(user)
})

// パブリッシュ
eventBus.Publish("user.created", user)
```

## 依存性注入

### 手動DI

```go
type App struct {
    userService *UserService
    fileService *FileService
    db          *sql.DB
}

func NewApp() *App {
    db := openDatabase()
    
    return &App{
        db:          db,
        userService: NewUserService(db),
        fileService: NewFileService(db),
    }
}
```

### Wireの使用

```go
// wire.go
//go:build wireinject

func InitializeApp() (*App, error) {
    wire.Build(
        openDatabase,
        NewUserService,
        NewFileService,
        NewApp,
    )
    return nil, nil
}
```

## 状態管理

### 集中型状態

```go
type AppState struct {
    currentUser *User
    settings    *Settings
    mu          sync.RWMutex
}

func (s *AppState) SetUser(user *User) {
    s.mu.Lock()
    defer s.mu.Unlock()
    s.currentUser = user
}

func (s *AppState) GetUser() *User {
    s.mu.RLock()
    defer s.mu.RUnlock()
    return s.currentUser
}
```

## ベストプラクティス

### ✅ 推奨

- 関心を分離する
- インターフェースを使用する
- 依存性を注入する
- エラーを適切に処理する
- サービスを焦点に保つ
- アーキテクチャを文書化する

### ❌ 非推奨

- グッドオブジェクトを作成しない
- コンポーネントを密結合にしない
- エラー処理をスキップしない
- 並行処理を無視しない
- 過剰設計しない

## 次のステップ

- [セキュリティ](/ja/guides/security) - セキュリティのベストプラクティス
- [ベストプラクティス](/ja/features/bindings/best-practices) - バインディングのベストプラクティス