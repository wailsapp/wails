---
title: Lista de Tarefas (TODO List)
description: Crie um aplicativo completo de lista de tarefas com operações de CRUD
sidebar:
  order: 2
---

import { Tabs, TabItem, Steps } from "@astrojs/starlight/components";
import { Image } from 'astro:assets';
import todoApp from "../../../../assets/todo-app.png";

Neste tutorial, você construirá um aplicativo de lista de tarefas (TODO list) totalmente funcional. Este é um passo em relação ao tutorial do Serviço de Código QR: você aprenderá a gerenciar o estado, lidar com múltiplas operações e criar uma interface de usuário polida.

**O que você construirá:**
- Um aplicativo TODO completo com funcionalidades de adicionar, concluir e excluir
- Gerenciamento de estado seguro para threads (importante para aplicativos de desktop)
- Design de UI moderno com estilo glassmorphism
- Tudo usando JavaScript puro (vanilla) - sem necessidade de frameworks

**O que você aprenderá:**
- Operações CRUD (Criar, Ler, Atualizar, Excluir)
- Gerenciamento seguro de estado mutável em Go
- Manipulação de entrada do usuário e validação
- Construção de interfaces responsivas que parecem nativas

<Image src={todoApp} alt="Aplicativo de Lista de Tarefas (TODO List)" style="max-width: 600px; margin: 2rem auto; display: block; border-radius: 8px; box-shadow: 0 4px 20px rgba(0,0,0,0.1);" />

**Tempo estimado:** 20 minutos

## Crie seu Projeto

<Steps>

1. **Gere o projeto**

   Primeiro, crie um novo projeto Wails. Usaremos o modelo vanilla padrão, que nos oferece um ponto de partida limpo:

   ```bash
   wails3 init -n todo-app
   cd todo-app
   ```

   Isso cria um novo projeto com a estrutura básica: backend Go na raiz, código frontend no diretório `frontend/`.

2. **Crie o serviço TODO**

   O serviço TODO gerenciará o estado do nosso aplicativo e fornecerá métodos para operações CRUD. Diferente de um servidor web, onde cada solicitação é isolada, aplicativos de desktop podem ter múltiplas operações concorrentes, por isso precisamos de gerenciamento de estado seguro para threads.

   Exclua `greetservice.go` e crie um novo arquivo `todoservice.go`:

   ```go title="todoservice.go"
   package main

   import (
       "errors"
       "sync"
   )

   type Todo struct {
       ID        int    `json:"id"`
       Title     string `json:"title"`
       Completed bool   `json:"completed"`
   }

   type TodoService struct {
       todos  []Todo
       nextID int
       mu     sync.RWMutex
   }

   func NewTodoService() *TodoService {
       return &TodoService{
           todos:  []Todo{},
           nextID: 1,
       }
   }

   func (t *TodoService) GetAll() []Todo {
       t.mu.RLock()
       defer t.mu.RUnlock()
       return t.todos
   }

   func (t *TodoService) Add(title string) (*Todo, error) {
       if title == "" {
           return nil, errors.New("title cannot be empty")
       }

       t.mu.Lock()
       defer t.mu.Unlock()

       todo := Todo{
           ID:        t.nextID,
           Title:     title,
           Completed: false,
       }
       t.todos = append(t.todos, todo)
       t.nextID++

       return &todo, nil
   }

   func (t *TodoService) Toggle(id int) error {
       t.mu.Lock()
       defer t.mu.Unlock()

       for i := range t.todos {
           if t.todos[i].ID == id {
               t.todos[i].Completed = !t.todos[i].Completed
               return nil
           }
       }
       return errors.New("todo not found")
   }

   func (t *TodoService) Delete(id int) error {
       t.mu.Lock()
       defer t.mu.Unlock()

       for i, todo := range t.todos {
           if todo.ID == id {
               t.todos = append(t.todos[:i], t.todos[i+1:]...)
               return nil
           }
       }
       return errors.New("todo not found")
   }
   ```

   **O que está acontecendo aqui:**

   **A estrutura `Todo`:**
   - Define a forma dos nossos dados com os campos ID, Title e Completed
   - As tags `json:` dizem ao Go como converter esta estrutura em JSON para o frontend
   - Cada campo é exportado (com letra maiúscula) para que o gerador de bindings possa acessá-lo

   **A estrutura `TodoService`:**
   - `todos []Todo` - uma fatia (slice) que armazena todos os nossos itens TODO
   - `nextID int` - rastreia o próximo ID a ser atribuído (simula incremento automático)
   - `mu sync.RWMutex` - um mutex de leitura/escrita para acesso seguro para threads

   **Segurança de threads com `sync.RWMutex`:**
   - Aplicativos de desktop podem ter múltiplas operações concorrentes vindas da UI
   - `RLock()` permite múltiplos leitores ao mesmo tempo (ex.: múltiplas chamadas `GetAll`)
   - `Lock()` dá acesso exclusivo para escritas (ex.: `Add`, `Toggle`, `Delete`)
   - `defer` garante que os locks sejam liberados mesmo se a função retornar antecipadamente ou causar um panic

   **Os métodos:**
   - `GetAll()` - Retorna todos os todos (usa lock de leitura pois não estamos modificando dados)
   - `Add(title)` - Cria um novo todo, valida a entrada, incrementa o ID
   - `Toggle(id)` - Inverte o status de conclusão de um todo
   - `Delete(id)` - Remove um todo da fatia

   **Tratamento de erros:**
   - Retornamos `error` como o último valor seguindo as convenções do Go
   - Títulos vazios são rejeitados
   - Operações em todos inexistentes retornam erros
   - Esses erros se tornam exceções JavaScript no frontend

3. **Atualize o main.go**

   Registre o serviço TODO com seu aplicativo Wails. Encontre a seção `Services` em `main.go` e substitua o GreetService pelo nosso TodoService:

   ```go title="main.go" {5}
   Services: []application.Service{
       application.NewService(NewTodoService()),
   },
   ```

   **O que está acontecendo aqui:**
   - Estamos removendo o GreetService padrão e adicionando nosso TodoService no lugar
   - `application.NewService()` envolve nosso serviço para que o Wails possa gerenciá-lo
   - O Wails gerará automaticamente bindings JavaScript para todos os métodos públicos deste serviço

4. **Crie a interface do frontend**

   Agora vamos construir o frontend. É aqui que chamaremos nossos métodos Go e exibiremos a UI. Estamos usando JavaScript puro para manter as coisas simples e mostrar como os bindings funcionam diretamente.

   Substitua `frontend/src/main.js`:

   ```javascript title="frontend/src/main.js"
   import {TodoService} from "../bindings/changeme";

   async function loadTodos() {
       const todos = await TodoService.GetAll();
       const list = document.getElementById('todo-list');

       list.innerHTML = todos.map(todo => `
           <div class="todo ${todo.completed ? 'completed' : ''}">
               <input type="checkbox"
                      ${todo.completed ? 'checked' : ''}
                      onchange="toggleTodo(${todo.id})">
               <span>${todo.title}</span>
               <button onclick="deleteTodo(${todo.id})">Delete</button>
           </div>
       `).join('');
   }

   window.addTodo = async () => {
       const input = document.getElementById('todo-input');
       const title = input.value.trim();

       if (title) {
           await TodoService.Add(title);
           input.value = '';
           await loadTodos();
       }
   }

   window.toggleTodo = async (id) => {
       await TodoService.Toggle(id);
       await loadTodos();
   }

   window.deleteTodo = async (id) => {
       await TodoService.Delete(id);
       await loadTodos();
   }

   // Load todos on startup
   loadTodos();
   ```

   **O que está acontecendo aqui:**

   **Importando os bindings:**
   - `import {TodoService} from "../bindings/changeme"` - traz os bindings Go gerados automaticamente
   - Nota: `changeme` será o nome real do seu módulo em `go.mod`

   **A função `loadTodos()`:**
   - Chama `TodoService.GetAll()` para buscar todos os todos do Go
   - Constrói o HTML para cada todo usando template literals
   - Adiciona/remove dinamicamente a classe `completed` para estilização
   - Usa atributos `onclick` para conectar os botões às nossas funções
   - Junta todo o HTML e o injeta no DOM

   **As funções CRUD:**
   - `addTodo()` - Valida a entrada, chama o método Go `Add`, atualiza a lista
   - `toggleTodo(id)` - Chama o método Go `Toggle`, atualiza a lista
   - `deleteTodo(id)` - Chama o método Go `Delete`, atualiza a lista
   - Todas as funções são assíncronas porque as chamadas Go retornam Promises**Por que anexar à janela:**
- `window.addTodo = ...` torna as funções acessíveis a partir de atributos `onclick` no HTML
- Este é um padrão simples para JavaScript puro (frameworks lidam com isso de maneira diferente)
- Em produção, você pode usar delegação de eventos adequada em vez disso

**O padrão de atualização:**
- Após cada mutação (adicionar/alternar/excluir), chamamos `loadTodos()` novamente
- Isso garante que a interface do usuário permaneça sincronizada com o estado do Go
- Alternativa: Fazer os métodos Go retornarem o novo estado para evitar a segunda chamada

5. **Atualizar o HTML**

O HTML fornece a estrutura para nosso aplicativo TODO. É minimalista e semântico — a mágica acontece no JavaScript e no CSS.

Substitua `frontend/index.html`:

```html title="frontend/index.html"
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title>TODO App</title>
    <link rel="stylesheet" href="./style.css"/>
</head>
<body>
    <div class="container">
        <h1>My TODOs</h1>
        <div class="card">
            <div class="input-box">
                <input type="text"
                       id="todo-input"
                       class="input"
                       placeholder="Add a new todo..."
                       onkeypress="if(event.key==='Enter') addTodo()">
                <button class="btn" onclick="addTodo()">Add</button>
            </div>
            <div id="todo-list"></div>
        </div>
    </div>
    <script type="module" src="./src/main.js"></script>
</body>
</html>
```

**O que está acontecendo aqui:**

**A estrutura:**
- `container` - centraliza nosso aplicativo e restringe a largura
- `card` - o cartão branco principal que contém tudo
- `input-box` - container flex para o campo de entrada e o botão Adicionar
- `todo-list` - onde os itens TODO individuais serão injetados pelo JavaScript

**Manipulação de eventos:**
- `onkeypress="if(event.key==='Enter') addTodo()"` - adiciona o item TODO quando Enter é pressionado
- `onclick="addTodo()"` - adiciona o item TODO quando o botão é clicado
- Manipuladores de eventos inline funcionam bem para aplicativos simples de JavaScript puro

**Script de módulo:**
- `<script type="module">` nos permite usar importações ES6
- Nosso arquivo `main.js` pode importar os bindings e usar JavaScript moderno

6. **Estilizar o aplicativo**

O CSS cria um design moderno e glassmorphic com transições suaves. Estamos buscando uma aparência refinada que torne o aplicativo agradável de usar.

Substitua `frontend/public/style.css`:

```css title="frontend/public/style.css"
:root {
    font-family: "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", "Roboto",
    "Oxygen", "Ubuntu", "Cantarell", "Fira Sans", "Droid Sans", "Helvetica Neue",
    sans-serif;
    font-size: 16px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: rgba(255, 255, 255, 0.87);
}

body {
    margin: 0;
    display: flex;
    place-items: center;
    justify-content: center;
    min-height: 100vh;
}

.container {
    width: 100%;
    max-width: 600px;
    padding: 20px;
}

h1 {
    text-align: center;
    color: white;
    font-size: 2.5em;
    font-weight: 300;
    margin: 0 0 30px 0;
    text-shadow: 0 2px 10px rgba(0, 0, 0, 0.2);
}

.card {
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(10px);
    border-radius: 16px;
    padding: 30px;
    box-shadow: 0 20px 60px rgba(0, 0, 0, 0.3);
}

.input-box {
    display: flex;
    gap: 10px;
    margin-bottom: 25px;
}

.input {
    flex: 1;
    border: 2px solid #e0e0e0;
    border-radius: 12px;
    height: 50px;
    padding: 0 20px;
    font-size: 16px;
    transition: all 0.3s ease;
}

.input:focus {
    border-color: #667eea;
    outline: none;
    box-shadow: 0 0 0 3px rgba(102, 126, 234, 0.1);
}

.btn {
    height: 50px;
    padding: 0 30px;
    border: none;
    border-radius: 12px;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
    color: white;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(102, 126, 234, 0.4);
}

.btn:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
}

#todo-list {
    display: flex;
    flex-direction: column;
    gap: 10px;
}

.todo {
    display: flex;
    align-items: center;
    padding: 18px 20px;
    background: white;
    border: 2px solid #f0f0f0;
    border-radius: 12px;
    transition: all 0.3s ease;
    gap: 15px;
}

.todo:hover {
    border-color: #667eea;
    box-shadow: 0 4px 12px rgba(102, 126, 234, 0.15);
    transform: translateX(4px);
}

.todo.completed {
    opacity: 0.6;
}

.todo.completed span {
    text-decoration: line-through;
    color: #999;
}

.todo input[type="checkbox"] {
    width: 24px;
    height: 24px;
    cursor: pointer;
    appearance: none;
    -webkit-appearance: none;
    border: 2px solid #667eea;
    border-radius: 6px;
    position: relative;
    transition: all 0.3s ease;
    flex-shrink: 0;
}

.todo input[type="checkbox"]:hover {
    background: rgba(102, 126, 234, 0.1);
}

.todo input[type="checkbox"]:checked {
    background: #667eea;
    border-color: #667eea;
}

.todo input[type="checkbox"]:checked::after {
    content: '✓';
    position: absolute;
    color: white;
    font-size: 16px;
    font-weight: bold;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
}

.todo span {
    flex: 1;
    font-size: 16px;
    color: #333;
}

.todo button {
    padding: 8px 16px;
    background: #ff4757;
    color: white;
    border: none;
    border-radius: 8px;
    font-size: 14px;
    font-weight: 600;
    cursor: pointer;
    transition: all 0.3s ease;
    opacity: 0;
    flex-shrink: 0;
}

.todo:hover button {
    opacity: 1;
}

.todo button:hover {
    background: #ee5a6f;
    transform: scale(1.05);
}

#todo-list:empty::before {
    content: "No todos yet. Add one above!";
    display: block;
    text-align: center;
    padding: 40px 20px;
    color: #999;
}
```

**O que está acontecendo aqui:**

**Design glassmorphic:**
- `background: linear-gradient(135deg, #667eea 0%, #764ba2 100%)` - fundo com gradiente roxo
- `backdrop-filter: blur(10px)` - cria o efeito de vidro fosco no cartão
- `rgba(255, 255, 255, 0.95)` - branco semitransparente para o efeito de vidro

**Estilização personalizada de checkbox:**
- `appearance: none` remove o checkbox padrão do navegador
- Criamos um quadrado arredondado personalizado com um checkmark usando `::after`
- O checkmark aparece quando `checked` usando um caractere unicode ✓

**Interações de hover:**
- Os itens TODO deslizam para a direita ao passar o mouse (`transform: translateX(4px)`)
- O botão de exclusão fica oculto até o hover (`opacity: 0` → `opacity: 1`)
- Os botões aumentam ligeiramente ao passar o mouse para dar feedback tátil

**Estado vazio:**
- `#todo-list:empty::before` exibe uma mensagem quando não há itens TODO
- Solução apenas em CSS - sem necessidade de JavaScript

7. **Executar o aplicativo**

Vamos vê-lo em ação! Execute o servidor de desenvolvimento:

```bash
wails3 dev
```

O aplicativo será compilado e aberto. Experimente:
- Digite um item TODO e pressione Enter ou clique em Adicionar
- Clique na caixa de seleção para marcá-lo como concluído
- Passe o mouse sobre um item TODO para ver o botão de exclusão aparecer
- Observe como a interface do usuário é atualizada instantaneamente — esse é nosso padrão de atualização funcionando**O que está acontecendo:**
- O Wails gerou automaticamente os bindings para os métodos do seu `TodoService`
- O modo de desenvolvimento inclui hot reload - tente alterar o CSS e veja a atualização em tempo real
- Seu código Go está sendo executado nativamente - sem necessidade de tradução ou interpretação

</Steps>

## Como Funciona

### Gerenciamento de Estado com Segurança de Thread

O `sync.RWMutex` fornece acesso seguro e concorrente:

```go
func (t *TodoService) GetAll() []Todo {
    t.mu.RLock()  // Read lock - multiple readers allowed
    defer t.mu.RUnlock()
    return t.todos
}

func (t *TodoService) Add(title string) (*Todo, error) {
    t.mu.Lock()  // Write lock - exclusive access
    defer t.mu.Unlock()
    // ... mutations
}
```

**Por que isso é importante:**
- Múltiplas chamadas do frontend podem ocorrer simultaneamente
- Operações de leitura não bloqueiam umas às outras
- Operações de escrita obtêm acesso exclusivo
- O `defer` garante que os bloqueios sejam sempre liberados

### Tratamento de Erros

O serviço retorna erros para operações inválidas:

```go
func (t *TodoService) Add(title string) (*Todo, error) {
    if title == "" {
        return nil, errors.New("title cannot be empty")
    }
    // ...
}
```

No frontend, você pode capturar esses erros:

```javascript
try {
    await TodoService.Add(title);
} catch (err) {
    alert('Error: ' + err);
}
```

### Sincronização de Estado

Após cada mutação, recarregamos a lista completa:

```javascript
window.addTodo = async () => {
    await TodoService.Add(title);  // Mutation
    await loadTodos();             // Refresh
}
```

**Abordagem alternativa:** Retorne a lista atualizada de cada método para evitar a segunda chamada.

## Melhorias

### Adicionar Estatísticas

Adicione isto ao `todoservice.go`:

```go
type TodoStats struct {
    Total     int `json:"total"`
    Completed int `json:"completed"`
    Active    int `json:"active"`
}

func (t *TodoService) GetStats() TodoStats {
    t.mu.RLock()
    defer t.mu.RUnlock()

    stats := TodoStats{
        Total: len(t.todos),
    }

    for _, todo := range t.todos {
        if todo.Completed {
            stats.Completed++
        } else {
            stats.Active++
        }
    }

    return stats
}
```

Exiba no frontend:

```javascript
async function loadTodos() {
    const [todos, stats] = await Promise.all([
        TodoService.GetAll(),
        TodoService.GetStats()
    ]);

    // Display stats
    document.getElementById('stats').textContent =
        `${stats.active} active, ${stats.completed} completed`;

    // ... render todos
}
```

### Adicionar "Limpar Concluídos"

```go
func (t *TodoService) ClearCompleted() int {
    t.mu.Lock()
    defer t.mu.Unlock()

    removed := 0
    newTodos := []Todo{}

    for _, todo := range t.todos {
        if !todo.Completed {
            newTodos = append(newTodos, todo)
        } else {
            removed++
        }
    }

    t.todos = newTodos
    return removed
}
```

### Adicionar Persistência

Para aplicativos de produção, você normalmente adicionaria persistência de banco de dados. Consulte o guia [Database Integration](/guides/patterns/database) para exemplos com SQLite, PostgreSQL, etc.

## Compilar para Produção

Quando estiver pronto para distribuir seu aplicativo TODO, compile-o para produção:

```bash
wails3 build
```

Isso cria um executável nativo otimizado em `build/bin/`:
- Compila seu código Go com otimizações
- Compila seu frontend para produção
- Agrupa tudo em um único executável
- O aplicativo resultante geralmente tem entre 10 e 20MB (compare com os 150MB+ do Electron)

Você pode executar o executável diretamente - sem necessidade de runtime, sem servidores para iniciar. É um verdadeiro aplicativo nativo.

## O Que Você Construiu

Você acabou de construir um aplicativo TODO completo com:

**Implementação completa de CRUD:**
- Criou um serviço com operações de Create, Read, Update e Delete
- Adicionou validação de entrada e tratamento de erros
- Aprendeu como erros do Go se tornam exceções em JavaScript

**Gerenciamento de estado com segurança de thread:**
- Usou `sync.RWMutex` para lidar com acesso concorrente de forma segura
- Entendeu a diferença entre bloqueios de leitura (RLock) e bloqueios de escrita (Lock)
- Viu como o `defer` evita deadlocks garantindo a limpeza

**Interface moderna e polida:**
- Construiu uma interface glassmorphic com gradientes e efeitos de desfoque
- Criou checkboxes com estilo personalizado sem nenhum framework
- Adicionou interações de hover e transições para uma sensação nativa
- Implementou um estado vazio com CSS puro

**Fundamentos do Wails:**
- Registro de serviço e geração automática de bindings
- Chamada de métodos Go do JavaScript com async/await
- Sincronização de estado entre Go e o frontend
- Compilação e empacotamento de um aplicativo desktop nativo

## Próximos Passos

Agora que você entende operações CRUD e gerenciamento de estado, tente:

- **Adicionar persistência:** Faça os todos sobreviverem às reinicializações do app com [SQLite](/guides/patterns/database)
- **Adicionar mais recursos:** Filtros (todos/ativos/concluídos), edição de todos existentes, operações em massa
- **Explorar o tutorial Notes:** Veja como as operações de arquivo funcionam em [Notes](/pt/tutorials/03-notes-vanilla)
- **Construa algo real:** Pegue esses conceitos e construa seu próprio aplicativo!