---
title: Otimização de Performance
description: Otimize sua aplicação Wails para máxima performance
sidebar:
  order: 9
---

## Visão Geral

Otimize sua aplicação Wails para velocidade, eficiência de memória e responsividade.

## Otimização do Frontend

### Tamanho do Bundle

```javascript
// vite.config.js
export default {
  build: {
    rollupOptions: {
      output: {
        manualChunks: {
          vendor: ['react', 'react-dom'],
        },
      },
    },
    minify: 'terser',
    terserOptions: {
      compress: {
        drop_console: true,
      },
    },
  },
}
```

### Code Splitting

```javascript
// Carregamento preguiçoso de componentes
const Settings = lazy(() => import('./Settings'))

function App() {
  return (
    <Suspense fallback={<Loading />}>
      <Settings />
    </Suspense>
  )
}
```

### Otimização de Ativos

```javascript
// Otimize imagens
import { defineConfig } from 'vite'
import imagemin from 'vite-plugin-imagemin'

export default defineConfig({
  plugins: [
    imagemin({
      gifsicle: { optimizationLevel: 3 },
      optipng: { optimizationLevel: 7 },
      svgo: { plugins: [{ removeViewBox: false }] },
    }),
  ],
})
```

## Otimização do Backend

### Bindings Eficientes

```go
// ❌ Ruim: Retornar tudo
func (s *Service) GetAllData() []Data {
    return s.db.FindAll() // Pode ser enorme
}

// ✅ Bom: Paginação
func (s *Service) GetData(page, size int) (*PagedData, error) {
    return s.db.FindPaged(page, size)
}
```

### Cache

```go
type CachedService struct {
    cache *lru.Cache
    ttl   time.Duration
}

func (s *CachedService) GetData(key string) (interface{}, error) {
    // Verificar cache
    if val, ok := s.cache.Get(key); ok {
        return val, nil
    }
    
    // Buscar e armazenar em cache
    data, err := s.fetchData(key)
    if err != nil {
        return nil, err
    }
    
    s.cache.Add(key, data)
    return data, nil
}
```

### Goroutines para Operações Longas

```go
func (s *Service) ProcessLargeFile(path string) error {
    // Processar em segundo plano
    go func() {
        result, err := s.process(path)
        if err != nil {
            s.app.Event.Emit("process-error", err.Error())
            return
        }
        s.app.Event.Emit("process-complete", result)
    }()
    
    return nil
}
```

## Otimização de Memória

### Evitar Vazamentos de Memória

```go
// ❌ Ruim: Vazamento de goroutine
func (s *Service) StartPolling() {
    ticker := time.NewTicker(1 * time.Second)
    go func() {
        for range ticker.C {
            s.poll()
        }
    }()
    // ticker nunca é parado!
}

// ✅ Bom: Limpeza adequada
func (s *Service) StartPolling() {
    ticker := time.NewTicker(1 * time.Second)
    s.stopChan = make(chan bool)
    
    go func() {
        for {
            select {
            case <-ticker.C:
                s.poll()
            case <-s.stopChan:
                ticker.Stop()
                return
            }
        }
    }()
}

func (s *Service) StopPolling() {
    close(s.stopChan)
}
```

### Recursos em Pool

```go
var bufferPool = sync.Pool{
    New: func() interface{} {
        return new(bytes.Buffer)
    },
}

func processData(data []byte) []byte {
    buf := bufferPool.Get().(*bytes.Buffer)
    defer bufferPool.Put(buf)
    
    buf.Reset()
    buf.Write(data)
    // Processar...
    return buf.Bytes()
}
```

## Otimização de Eventos

### Debounce de Eventos

```javascript
// Debounce em eventos frequentes
let debounceTimer
function handleInput(value) {
    clearTimeout(debounceTimer)
    debounceTimer = setTimeout(() => {
        UpdateData(value)
    }, 300)
}
```

### Atualizações em Lote

```go
type BatchProcessor struct {
    items []Item
    mu    sync.Mutex
    timer *time.Timer
}

func (b *BatchProcessor) Add(item Item) {
    b.mu.Lock()
    defer b.mu.Unlock()
    
    b.items = append(b.items, item)
    
    if b.timer == nil {
        b.timer = time.AfterFunc(100*time.Millisecond, b.flush)
    }
}

func (b *BatchProcessor) flush() {
    b.mu.Lock()
    items := b.items
    b.items = nil
    b.timer = nil
    b.mu.Unlock()
    
    // Processar lote
    processBatch(items)
}
```

## Otimização de Build

### Tamanho do Binário

```bash
# Remover símbolos de depuração
wails3 build -ldflags "-s -w"

# Reduzir ainda mais o tamanho do binário
go build -ldflags="-s -w" -trimpath
```

### Velocidade de Compilação

```bash
# Usar cache de build
go build -buildmode=default

# Compilação paralela
go build -p 8
```

## Profiling

### Profiling de CPU

```go
import "runtime/pprof"

func profileCPU() {
    f, _ := os.Create("cpu.prof")
    defer f.Close()
    
    pprof.StartCPUProfile(f)
    defer pprof.StopCPUProfile()
    
    // Código para perfil
}
```

### Profiling de Memória

```go
import "runtime/pprof"

func profileMemory() {
    f, _ := os.Create("mem.prof")
    defer f.Close()
    
    runtime.GC()
    pprof.WriteHeapProfile(f)
}
```

### Analisar Perfis

```bash
# Visualizar perfil de CPU
go tool pprof cpu.prof

# Visualizar perfil de memória
go tool pprof mem.prof

# Interface web
go tool pprof -http=:8080 cpu.prof
```

## Melhores Práticas

### ✅ Faça

- Faça profiling antes de otimizar
- Armazene em cache operações custosas
- Use paginação para grandes conjuntos de dados
- Use debounce em eventos frequentes
- Use recursos em pool
- Limpe goroutines
- Otimize o tamanho do bundle
- Use carregamento preguiçoso (lazy loading)

### ❌ Não Faça

- Não otimize prematuramente
- Não ignore vazamentos de memória
- Não bloqueie a thread principal
- Não retorne grandes conjuntos de dados
- Não pule o profiling
- Não esqueça a limpeza

## Checklist de Performance

- [ ] Bundle do frontend otimizado
- [ ] Imagens comprimidas
- [ ] Code splitting implementado
- [ ] Métodos do backend paginados
- [ ] Cache implementado
- [ ] Goroutines limpas
- [ ] Eventos com debounce
- [ ] Tamanho do binário otimizado
- [ ] Profiling realizado
- [ ] Vazamentos de memória corrigidos

## Próximos Passos

- [Arquitetura](/pt/guides/architecture) - Padrões de arquitetura de aplicação
- [Testes](/pt/guides/testing) - Teste sua aplicação
- [Build](/guides/building) - Construa binários otimizados