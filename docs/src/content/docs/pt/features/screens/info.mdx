---
title: Informações da Tela
description: Obtenha informações sobre exibições e monitores
sidebar:
  order: 1
---

import { Card, CardGrid } from "@astrojs/starlight/components";

## Informações da Tela

O Wails oferece uma **API unificada de tela** que funciona em todas as plataformas. Obtenha informações da tela, detecte múltiplos monitores, consulte propriedades da tela (tamanho, posição, DPI), identifique a exibição primária e lide com a escala de DPI com código consistente.

## Início Rápido

```go
// Obter todas as telas
screens := app.Screen.GetAll()

for _, screen := range screens {
    fmt.Printf("Tela: %s (%dx%d)\n", 
        screen.Name, screen.Width, screen.Height)
}

// Obter tela primária
primary := app.Screens.GetPrimary()
fmt.Printf("Primária: %s\n", primary.Name)
```

**É isso!** Informações de tela multiplataforma.

## Obtendo Informações da Tela

### Todas as Telas

```go
screens := app.Screen.GetAll()

for _, screen := range screens {
    fmt.Printf("ID: %s\n", screen.ID)
    fmt.Printf("Nome: %s\n", screen.Name)
    fmt.Printf("Tamanho: %dx%d\n", screen.Width, screen.Height)
    fmt.Printf("Posição: %d,%d\n", screen.X, screen.Y)
    fmt.Printf("Escala: %.2f\n", screen.ScaleFactor)
    fmt.Printf("Primária: %v\n", screen.IsPrimary)
    fmt.Println("---")
}
```

### Tela Primária

```go
primary := app.Screens.GetPrimary()

fmt.Printf("Tela primária: %s\n", primary.Name)
fmt.Printf("Resolução: %dx%d\n", primary.Width, primary.Height)
fmt.Printf("Fator de escala: %.2f\n", primary.ScaleFactor)
```

### Tela Atual

Obtenha a tela que contém uma janela:

```go
screen := app.Screens.GetCurrent(window)

fmt.Printf("A janela está em: %s\n", screen.Name)
```

### Tela por ID

```go
screen := app.Screens.GetByID("screen-id")
if screen != nil {
    fmt.Printf("Tela encontrada: %s\n", screen.Name)
}
```

## Propriedades da Tela

### Estrutura da Tela

```go
type Screen struct {
    ID          string  // Identificador único
    Name        string  // Nome da exibição
    X           int     // Posição X
    Y           int     // Posição Y
    Width       int     // Largura em pixels
    Height      int     // Altura em pixels
    ScaleFactor float32 // Escala de DPI (1.0, 1.5, 2.0, etc.)
    IsPrimary   bool    // Esta é a tela primária?
}
```

### Pixels Físicos vs. Lógicos

```go
screen := app.Screens.GetPrimary()

// Pixels lógicos (o que você usa)
logicalWidth := screen.Width
logicalHeight := screen.Height

// Pixels físicos (exibição real)
physicalWidth := int(float32(screen.Width) * screen.ScaleFactor)
physicalHeight := int(float32(screen.Height) * screen.ScaleFactor)

fmt.Printf("Lógico: %dx%d\n", logicalWidth, logicalHeight)
fmt.Printf("Físico: %dx%d\n", physicalWidth, physicalHeight)
fmt.Printf("Escala: %.2f\n", screen.ScaleFactor)
```

**Fatores de escala comuns:**
- `1.0` - DPI Padrão (96 DPI)
- `1.25` - Escala de 125% (120 DPI)
- `1.5` - Escala de 150% (144 DPI)
- `2.0` - Escala de 200% (192 DPI) - Retina
- `3.0` - Escala de 300% (288 DPI) - 4K/5K

## Posicionamento de Janelas

### Centralizar na Tela

```go
func centreOnScreen(window *application.WebviewWindow, screen *Screen) {
    windowWidth, windowHeight := window.Size()
    
    x := screen.X + (screen.Width-windowWidth)/2
    y := screen.Y + (screen.Height-windowHeight)/2
    
    window.SetPosition(x, y)
}
```

### Posicionar em Tela Específica

```go
func moveToScreen(window *application.WebviewWindow, screenIndex int) {
    screens := app.Screen.GetAll()
    
    if screenIndex < 0 || screenIndex >= len(screens) {
        return
    }
    
    screen := screens[screenIndex]
    
    // Centralizar na tela de destino
    centreOnScreen(window, screen)
}
```

### Posicionar Relativo à Tela

```go
// Canto superior esquerdo
func positionTopLeft(window *application.WebviewWindow, screen *Screen) {
    window.SetPosition(screen.X+10, screen.Y+10)
}

// Canto superior direito
func positionTopRight(window *application.WebviewWindow, screen *Screen) {
    windowWidth, _ := window.Size()
    window.SetPosition(screen.X+screen.Width-windowWidth-10, screen.Y+10)
}

// Canto inferior direito
func positionBottomRight(window *application.WebviewWindow, screen *Screen) {
    windowWidth, windowHeight := window.Size()
    window.SetPosition(
        screen.X+screen.Width-windowWidth-10,
        screen.Y+screen.Height-windowHeight-10,
    )
}
```

## Suporte a Múltiplos Monitores

### Detectar Múltiplos Monitores

```go
func hasMultipleMonitors() bool {
    return len(app.Screen.GetAll()) > 1
}

func getMonitorCount() int {
    return len(app.Screen.GetAll())
}
```

### Listar Todos os Monitores

```go
func listMonitors() {
    screens := app.Screen.GetAll()
    
    fmt.Printf("Encontrados %d monitor(es):\n", len(screens))
    
    for i, screen := range screens {
        primary := ""
        if screen.IsPrimary {
            primary = " (Primário)"
        }
        
        fmt.Printf("%d. %s%s\n", i+1, screen.Name, primary)
        fmt.Printf("   Resolução: %dx%d\n", screen.Width, screen.Height)
        fmt.Printf("   Posição: %d,%d\n", screen.X, screen.Y)
        fmt.Printf("   Escala: %.2fx\n", screen.ScaleFactor)
    }
}
```

### Escolher Monitor

```go
func chooseMonitor() (*Screen, error) {
    screens := app.Screen.GetAll()
    
    if len(screens) == 1 {
        return screens[0], nil
    }
    
    // Exibir diálogo para escolher
    var options []string
    for i, screen := range screens {
        primary := ""
        if screen.IsPrimary {
            primary = " (Primário)"
        }
        options = append(options, 
            fmt.Sprintf("%d. %s%s - %dx%d", 
                i+1, screen.Name, primary, screen.Width, screen.Height))
    }
    
    // Usar diálogo para selecionar
    // (A implementação depende do seu sistema de diálogo)
    
    return screens[0], nil
}
```

## Exemplos Completos

### Gerenciador de Janelas Multi-Monitor

```go
type MultiMonitorManager struct {
    app     *application.Application
    windows map[int]*application.WebviewWindow
}

func NewMultiMonitorManager(app *application.Application) *MultiMonitorManager {
    return &MultiMonitorManager{
        app:     app,
        windows: make(map[int]*application.WebviewWindow),
    }
}

func (m *MultiMonitorManager) CreateWindowOnScreen(screenIndex int) error {
    screens := m.app.Screen.GetAll()
    
    if screenIndex < 0 || screenIndex >= len(screens) {
        return errors.New("índice de tela inválido")
    }
    
    screen := screens[screenIndex]
    
    // Criar janela
    window := m.app.Window.NewWithOptions(application.WebviewWindowOptions{
        Title:  fmt.Sprintf("Janela em %s", screen.Name),
        Width:  800,
        Height: 600,
    })
    
    // Centralizar na tela
    x := screen.X + (screen.Width-800)/2
    y := screen.Y + (screen.Height-600)/2
    window.SetPosition(x, y)
    
    window.Show()
    
    m.windows[screenIndex] = window
    return nil
}

func (m *MultiMonitorManager) CreateWindowOnEachScreen() {
    screens := m.app.Screen.GetAll()
    
    for i := range screens {
        m.CreateWindowOnScreen(i)
    }
}
```

### Detecção de Mudança de Tela

```go
type ScreenMonitor struct {
    app           *application.Application
    lastScreens   []*Screen
    changeHandler func([]*Screen)
}

func NewScreenMonitor(app *application.Application) *ScreenMonitor {
    return &ScreenMonitor{
        app:         app,
        lastScreens: app.Screen.GetAll(),
    }
}

func (sm *ScreenMonitor) OnScreenChange(handler func([]*Screen)) {
    sm.changeHandler = handler
}

func (sm *ScreenMonitor) Start() {
    ticker := time.NewTicker(2 * time.Second)
    
    go func() {
        for range ticker.C {
            sm.checkScreens()
        }
    }()
}

func (sm *ScreenMonitor) checkScreens() {
    current := sm.app.Screen.GetAll()
    
    if len(current) != len(sm.lastScreens) {
        sm.lastScreens = current
        if sm.changeHandler != nil {
            sm.changeHandler(current)
        }
    }
}
```

### Dimensionamento de Janela Sensível ao DPI

```go
func createDPIAwareWindow(screen *Screen) *application.WebviewWindow {
    // Tamanho base na escala 1.0---

**Perguntas?** Pergunte no [Discord](https://discord.gg/JDdSxwjhGf) ou confira os [exemplos de tela](https://github.com/wailsapp/wails/tree/master/v3/examples).