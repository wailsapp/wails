---
title: Контекстные меню
description: Создавайте контекстные меню правой кнопки мыши для вашего приложения
sidebar:
  order: 2
---

import { Card, CardGrid, Tabs, TabItem } from "@astrojs/starlight/components";

## Проблема

Пользователи ожидают появления контекстных меню правой кнопки мыши с действиями, зависящими от контекста. Для разных элементов требуются разные меню:
- **Текст**: Вырезать, Копировать, Вставить
- **Изображения**: Сохранить, Копировать, Открыть
- **Пользовательские элементы**: Специфичные для приложения действия

Создание контекстных меню вручную означает обработку событий мыши, позиционирование и учёт различий между платформами.

## Решение Wails

Wails предоставляет **декларативные контекстные меню** с использованием CSS-свойств. Связывайте меню с HTML-элементами, передавайте данные и обрабатывайте клики — всё это с нативным поведением платформы.

## Быстрый старт

**Код Go:**

```go
// Create context menu
contextMenu := app.NewContextMenu()
contextMenu.Add("Cut").OnClick(handleCut)
contextMenu.Add("Copy").OnClick(handleCopy)
contextMenu.Add("Paste").OnClick(handlePaste)

// Register with ID
app.RegisterContextMenu("editor-menu", contextMenu)
```

**HTML:**

```html
<textarea style="--custom-contextmenu: editor-menu">
    Right-click me!
</textarea>
```

**Вот и всё!** При нажатии правой кнопкой мыши на текстовом поле появится ваше пользовательское меню.

## Создание контекстных меню

### Базовое контекстное меню

```go
// Create menu
contextMenu := app.NewContextMenu()

// Add items
contextMenu.Add("Cut").SetAccelerator("CmdOrCtrl+X").OnClick(func(ctx *application.Context) {
    // Handle cut
})

contextMenu.Add("Copy").SetAccelerator("CmdOrCtrl+C").OnClick(func(ctx *application.Context) {
    // Handle copy
})

contextMenu.Add("Paste").SetAccelerator("CmdOrCtrl+V").OnClick(func(ctx *application.Context) {
    // Handle paste
})

// Register with unique ID
app.RegisterContextMenu("text-menu", contextMenu)
```

**ID меню:** Должен быть уникальным. Используется для связывания меню с HTML-элементами.

### С подменю

```go
contextMenu := app.NewContextMenu()

// Add regular items
contextMenu.Add("Open").OnClick(handleOpen)
contextMenu.Add("Delete").OnClick(handleDelete)

contextMenu.AddSeparator()

// Add submenu
exportMenu := contextMenu.AddSubmenu("Export As")
exportMenu.Add("PNG").OnClick(exportPNG)
exportMenu.Add("JPEG").OnClick(exportJPEG)
exportMenu.Add("SVG").OnClick(exportSVG)

app.RegisterContextMenu("image-menu", contextMenu)
```

### С флажками и радиогруппами

```go
contextMenu := app.NewContextMenu()

// Checkbox
contextMenu.AddCheckbox("Show Grid", true).OnClick(func(ctx *application.Context) {
    showGrid := ctx.ClickedMenuItem().Checked()
    // Toggle grid
})

contextMenu.AddSeparator()

// Radio group
contextMenu.AddRadio("Small", false).OnClick(handleSize)
contextMenu.AddRadio("Medium", true).OnClick(handleSize)
contextMenu.AddRadio("Large", false).OnClick(handleSize)

app.RegisterContextMenu("view-menu", contextMenu)
```

**Для всех типов элементов меню** см. [Справочник по меню](/ru/features/menus/reference).

## Связывание с HTML-элементами

Используйте пользовательские CSS-свойства для прикрепления контекстных меню:

### Базовая ассоциация

```html
<div style="--custom-contextmenu: menu-id">
    Right-click me!
</div>
```

**CSS-свойство:** `--custom-contextmenu: <menu-id>`

### С контекстными данными

Передавайте данные из HTML в Go:

```html
<div style="--custom-contextmenu: file-menu; --custom-contextmenu-data: file-123">
    Right-click this file
</div>
```

**Обработчик Go:**

```go
contextMenu := app.NewContextMenu()
contextMenu.Add("Open").OnClick(func(ctx *application.Context) {
    fileID := ctx.ContextMenuData()  // "file-123"
    openFile(fileID)
})

app.RegisterContextMenu("file-menu", contextMenu)
```

**CSS-свойства:**
- `--custom-contextmenu: <menu-id>` — какое меню показать
- `--custom-contextmenu-data: <data>` — данные для передачи обработчикам

### Динамические данные

Генерируйте данные динамически в JavaScript:

```html
<div id="file-item" style="--custom-contextmenu: file-menu">
    File.txt
</div>

<script>
// Set data dynamically
const fileItem = document.getElementById('file-item')
fileItem.style.setProperty('--custom-contextmenu-data', 'file-' + fileId)
</script>
```

### Несколько элементов, одно меню

```html
<div class="file-item" style="--custom-contextmenu: file-menu; --custom-contextmenu-data: file-1">
    Document.pdf
</div>

<div class="file-item" style="--custom-contextmenu: file-menu; --custom-contextmenu-data: file-2">
    Image.png
</div>

<div class="file-item" style="--custom-contextmenu: file-menu; --custom-contextmenu-data: file-3">
    Video.mp4
</div>
```

**Одно меню, разные данные для каждого элемента.**

## Контекстные данные

### Доступ к контекстным данным

```go
contextMenu.Add("Process").OnClick(func(ctx *application.Context) {
    data := ctx.ContextMenuData()  // Get data from HTML
    
    // Use the data
    processItem(data)
})
```

**Тип данных:** Всегда `string`. Парсите по мере необходимости.

### Передача сложных данных

Используйте JSON для сложных данных:

```html
<div style="--custom-contextmenu: item-menu; --custom-contextmenu-data: {&quot;id&quot;:123,&quot;type&quot;:&quot;image&quot;}">
    Image.png
</div>
```

**Обработчик Go:**

```go
import "encoding/json"

type ItemData struct {
    ID   int    `json:"id"`
    Type string `json:"type"`
}

contextMenu.Add("Process").OnClick(func(ctx *application.Context) {
    dataStr := ctx.ContextMenuData()
    
    var data ItemData
    if err := json.Unmarshal([]byte(dataStr), &data); err != nil {
        log.Printf("Invalid data: %v", err)
        return
    }
    
    processItem(data.ID, data.Type)
})
```

:::caution[Безопасность]
**Всегда проверяйте контекстные данные** из фронтенда. Пользователи могут манипулировать CSS-свойствами, поэтому относитесь к данным как к недоверенному вводу.
:::

### Пример валидации

```go
contextMenu.Add("Delete").OnClick(func(ctx *application.Context) {
    fileID := ctx.ContextMenuData()
    
    // Validate
    if !isValidFileID(fileID) {
        log.Printf("Invalid file ID: %s", fileID)
        return
    }
    
    // Check permissions
    if !canDeleteFile(fileID) {
        showError("Permission denied")
        return
    }
    
    // Safe to proceed
    deleteFile(fileID)
})
```

## Контекстное меню по умолчанию

WebView предоставляет встроенное контекстное меню для стандартных операций (копировать, вставить, инспектор). Управляйте им с помощью `--default-contextmenu`:

### Скрыть меню по умолчанию

```html
<div style="--default-contextmenu: hide">
    No default menu here
</div>
```

**Сценарий использования:** Пользовательские элементы интерфейса, где меню по умолчанию не имеет смысла.

### Показать меню по умолчанию

```html
<div style="--default-contextmenu: show">
    Default menu always shown
</div>
```

**Сценарий использования:** Текстовые области, поля ввода, редактируемый контент.

### Автоматический (умный) режим

```html
<div style="--default-contextmenu: auto">
    Smart context menu
</div>
```

**Поведение по умолчанию.** Показывает меню по умолчанию, когда:
- Выбран текст
- В полях ввода текста
- В редактируемом контенте (`contenteditable`)

В остальных случаях скрывает меню по умолчанию.

### Комбинирование пользовательского и стандартного

```html
<!-- Custom menu + default menu -->
<textarea style="--custom-contextmenu: editor-menu; --default-contextmenu: show">
    Both menus available
</textarea>
```

**Поведение:**
1. Сначала показывается пользовательское меню
2. Если пользовательское меню пусто или не найдено, показывается меню по умолчанию
3. Оба могут сосуществовать (зависит от платформы)

## Динамические контекстные меню

Обновляйте меню в зависимости от состояния приложения:

### Включение/отключение элементов

```go
var cutMenuItem *application.MenuItem
var copyMenuItem *application.MenuItem

func createContextMenu() {
    contextMenu := app.NewContextMenu()
    
    cutMenuItem = contextMenu.Add("Cut")
    cutMenuItem.SetEnabled(false)  // Initially disabled
    cutMenuItem.OnClick(handleCut)
    
    copyMenuItem = contextMenu.Add("Copy")
    copyMenuItem.SetEnabled(false)
    copyMenuItem.OnClick(handleCopy)
    
    app.RegisterContextMenu("editor-menu", contextMenu)
}

func onSelectionChanged(hasSelection bool) {
    cutMenuItem.SetEnabled(hasSelection)
    copyMenuItem.SetEnabled(hasSelection)
    contextMenu.Update()  // Important!
}
```

:::caution[Всегда вызывайте Update()]После изменения состояния меню **вызовите `contextMenu.Update()`**. Это критически важно для Windows.

Подробнее см. в [Справочнике по меню](/features/menus/reference#enabled-state).
:::

### Изменение меток

```go
playMenuItem := contextMenu.Add("Play")

playMenuItem.OnClick(func(ctx *application.Context) {
    if isPlaying {
        playMenuItem.SetLabel("Pause")
    } else {
        playMenuItem.SetLabel("Play")
    }
    contextMenu.Update()
})
```

### Пересборка меню

Для значительных изменений пересоберите всё меню заново:

```go
func rebuildContextMenu(fileType string) {
    contextMenu := app.NewContextMenu()
    
    // Общие пункты
    contextMenu.Add("Open").OnClick(handleOpen)
    contextMenu.Add("Delete").OnClick(handleDelete)
    
    contextMenu.AddSeparator()
    
    // Пункты, зависящие от типа
    switch fileType {
    case "image":
        contextMenu.Add("Edit Image").OnClick(editImage)
        contextMenu.Add("Set as Wallpaper").OnClick(setWallpaper)
    case "video":
        contextMenu.Add("Play").OnClick(playVideo)
        contextMenu.Add("Extract Audio").OnClick(extractAudio)
    case "document":
        contextMenu.Add("Print").OnClick(printDocument)
        contextMenu.Add("Export PDF").OnClick(exportPDF)
    }
    
    app.RegisterContextMenu("file-menu", contextMenu)
}
```

## Поведение на разных платформах

Контекстные меню **нативны для платформы**:

<Tabs syncKey="platform">
  <TabItem label="macOS" icon="apple">
    **Нативные контекстные меню macOS:**
    - Системные анимации и переходы
    - Правый клик = Control+Click (автоматически)
    - Адаптация к системной теме (светлая/темная)
    - Стандартные операции с текстом в меню по умолчанию
    - Нативная прокрутка для длинных меню
    
    **Конвенции macOS:**
    - Используйте заглавную только первую букву для пунктов меню
    - Используйте многоточие (...) для пунктов, открывающих диалоги
    - Распространенные сочетания клавиш: ⌘C (Копировать), ⌘V (Вставить)
  </TabItem>

  <TabItem label="Windows" icon="seti:windows">
    **Нативные контекстные меню Windows:**
    - Стиль Windows
    - Следование теме Windows
    - Стандартные операции Windows в меню по умолчанию
    - Поддержка ввода с сенсорного экрана и пера
    
    **Конвенции Windows:**
    - Используйте заглавную первую букву для пунктов меню
    - Используйте многоточие (...) для пунктов, открывающих диалоги
    - Распространенные сочетания клавиш: Ctrl+C (Копировать), Ctrl+V (Вставить)
  </TabItem>

  <TabItem label="Linux" icon="linux">
    **Интеграция с окружением рабочего стола:**
    - Адаптация к теме рабочего стола (GTK, Qt и т.д.)
    - Поведение правого клика следует настройкам системы
    - Содержимое меню по умолчанию зависит от окружения
    - Позиционирование следует конвенциям DE
    
    **Особенности Linux:**
    - Тестируйте на целевых окружениях рабочего стола
    - GTK и Qt имеют разное поведение
    - Некоторые DE настраивают контекстные меню
  </TabItem>
</Tabs>

## Полный пример

**Код Go:**

```go
package main

import (
    "encoding/json"
    "log"
    "github.com/wailsapp/wails/v3/pkg/application"
)

type FileData struct {
    ID   string `json:"id"`
    Type string `json:"type"`
    Name string `json:"name"`
}

func main() {
    app := application.New(application.Options{
        Name: "Context Menu Demo",
    })

    // Создаем контекстное меню для файлов
    fileMenu := createFileMenu(app)
    app.RegisterContextMenu("file-menu", fileMenu)

    // Создаем контекстное меню для изображений
    imageMenu := createImageMenu(app)
    app.RegisterContextMenu("image-menu", imageMenu)

    // Создаем контекстное меню для текста
    textMenu := createTextMenu(app)
    app.RegisterContextMenu("text-menu", textMenu)

    app.Window.New()
    app.Run()
}

func createFileMenu(app *application.Application) *application.ContextMenu {
    menu := app.NewContextMenu()
    
    menu.Add("Open").OnClick(func(ctx *application.Context) {
        data := parseFileData(ctx.ContextMenuData())
        openFile(data.ID)
    })
    
    menu.Add("Rename").OnClick(func(ctx *application.Context) {
        data := parseFileData(ctx.ContextMenuData())
        renameFile(data.ID)
    })
    
    menu.AddSeparator()
    
    menu.Add("Delete").OnClick(func(ctx *application.Context) {
        data := parseFileData(ctx.ContextMenuData())
        deleteFile(data.ID)
    })
    
    return menu
}

func createImageMenu(app *application.Application) *application.ContextMenu {
    menu := app.NewContextMenu()
    
    menu.Add("View").OnClick(func(ctx *application.Context) {
        data := parseFileData(ctx.ContextMenuData())
        viewImage(data.ID)
    })
    
    menu.Add("Edit").OnClick(func(ctx *application.Context) {
        data := parseFileData(ctx.ContextMenuData())
        editImage(data.ID)
    })
    
    menu.AddSeparator()
    
    exportMenu := menu.AddSubmenu("Export As")
    exportMenu.Add("PNG").OnClick(exportPNG)
    exportMenu.Add("JPEG").OnClick(exportJPEG)
    exportMenu.Add("WebP").OnClick(exportWebP)
    
    return menu
}

func createTextMenu(app *application.Application) *application.ContextMenu {
    menu := app.NewContextMenu()
    
    menu.Add("Cut").SetAccelerator("CmdOrCtrl+X").OnClick(handleCut)
    menu.Add("Copy").SetAccelerator("CmdOrCtrl+C").OnClick(handleCopy)
    menu.Add("Paste").SetAccelerator("CmdOrCtrl+V").OnClick(handlePaste)
    
    return menu
}

func parseFileData(dataStr string) FileData {
    var data FileData
    if err := json.Unmarshal([]byte(dataStr), &data); err != nil {
        log.Printf("Invalid file data: %v", err)
    }
    return data
}

// Реализация обработчиков...
func openFile(id string) { /* ... */ }
func renameFile(id string) { /* ... */ }
func deleteFile(id string) { /* ... */ }
func viewImage(id string) { /* ... */ }
func editImage(id string) { /* ... */ }
func exportPNG(ctx *application.Context) { /* ... */ }
func exportJPEG(ctx *application.Context) { /* ... */ }
func exportWebP(ctx *application.Context) { /* ... */ }
func handleCut(ctx *application.Context) { /* ... */ }
func handleCopy(ctx *application.Context) { /* ... */ }
func handlePaste(ctx *application.Context) { /* ... */ }
```

**HTML:**

```html
<!DOCTYPE html>
<html>
<head>
    <style>
        .file-item {
            padding: 10px;
            margin: 5px;
            border: 1px solid #ccc;
            cursor: pointer;
        }
        
        .file-item:hover {
            background: #f0f0f0;
        }
        
        textarea {
            width: 100%;
            height: 200px;
        }
    </style>
</head>
<body>
    <h2>Файлы</h2>
    
    <!-- Обычный файл -->
    <div class="file-item" 
         style="--custom-contextmenu: file-menu; 
                --custom-contextmenu-data: {&quot;id&quot;:&quot;file-1&quot;,&quot;type&quot;:&quot;document&quot;,&quot;name&quot;:&quot;Report.pdf&quot;}">
        📄 Report.pdf
    </div>
    
    <!-- Файл изображения -->
    <div class="file-item" 
         style="--custom-contextmenu: image-menu; 
                --custom-contextmenu-data: {&quot;id&quot;:&quot;file-2&quot;,&quot;type&quot;:&quot;image&quot;,&quot;name&quot;:&quot;Photo.jpg&quot;}">
        🖼️ Photo.jpg
    </div>
    
    <h2>Текстовый редактор</h2>
    
    <!-- Текстовое поле с пользовательским меню + меню по умолчанию -->
    <textarea 
        style="--custom-contextmenu: text-menu; --default-contextmenu: show"
        placeholder="Введите текст, затем нажмите правую кнопку мыши...">
    </textarea>
    
    <h2>Без контекстного меню</h2>
    
    <!-- Отключение меню по умолчанию -->
    <div style="--default-contextmenu: hide; padding: 20px; border: 1px solid #ccc;">
        Нажмите правую кнопку мыши здесь — меню не появится
    </div>
</body>
</html>
```

## Лучшие практики

### ✅ Что делать

- **Держите меню сфокусированными** — только релевантные действия для элемента
- **Проверяйте контекстные данные** — относитесь к ним как к недоверенному вводу
- **Используйте понятные метки** — "Удалить файл", а не просто "Удалить"
- **Вызывайте menu.Update()** — после изменения состояния меню
- **Тестируйте на всех платформах** — поведение различается
- **Предоставляйте сочетания клавиш** — для распространенных действий
- **Группируйте связанные пункты** — используйте разделители

### ❌ Чего не делать

- **Не доверяйте контекстным данным** — всегда проверяйте
- **Не делайте меню слишком длинными** — максимум 7-10 пунктов
- **Не забывайте про menu.Update()** — меню не будут работать корректно---

**Вопросы?** Задавайте их в [Discord](https://discord.gg/JDdSxwjhGf) или смотрите [пример контекстного меню](https://github.com/wailsapp/wails/tree/master/v3/examples/contextmenus).