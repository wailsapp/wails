version: '3'

vars:
  APP_NAME: "{{.BinaryName}}"
  BIN_DIR: "bin"
  PACKAGE_MANAGER: '{{.Opn}}.PACKAGE_MANAGER | default "npm"{{.Cls}}'
  VITE_PORT: '{{.Opn}}.WAILS_VITE_PORT | default 9245{{.Cls}}'
  # Target OS for build/package/run. Defaults to the host OS, and is overridden
  # by `wails3 build GOOS=...` (or the GOOS env var) for cross-compilation. The
  # tasks below dispatch to the matching platform Taskfile via this variable.
  GOOS: '{{.Opn}}.GOOS | default OS{{.Cls}}'

  ## ─── Build Customization ──────────────────────────────────────────────
  ## These variables are NOT overwritten by `wails3 update build-assets`.
  ## All values are additive — empty means no change from platform defaults.
  ## Override via environment: APP_TAGS="extra" wails3 task build

  # Tags included in every Taskfile-driven build
  APP_TAGS: '{{.Opn}}.APP_TAGS | default ""{{.Cls}}'
  # Platform-specific tags (only applied on that platform's builds)
  APP_TAGS_LINUX: '{{.Opn}}.APP_TAGS_LINUX | default ""{{.Cls}}'
  APP_TAGS_DARWIN: '{{.Opn}}.APP_TAGS_DARWIN | default ""{{.Cls}}'
  APP_TAGS_WINDOWS: '{{.Opn}}.APP_TAGS_WINDOWS | default ""{{.Cls}}'
  APP_TAGS_ANDROID: '{{.Opn}}.APP_TAGS_ANDROID | default ""{{.Cls}}'
  APP_TAGS_IOS: '{{.Opn}}.APP_TAGS_IOS | default ""{{.Cls}}'
  APP_TAGS_SERVER: '{{.Opn}}.APP_TAGS_SERVER | default ""{{.Cls}}'
  # Linker flags appended to every Taskfile-driven build (e.g. -X main.Version=1.0.0)
  APP_LDFLAGS: '{{.Opn}}.APP_LDFLAGS | default ""{{.Cls}}'
  # Desktop/server CGO override. Empty uses linux/darwin=1 and windows=0;
  # native server builds keep Go's host default, server Docker uses 0, and
  # mobile toolchains always require CGO_ENABLED=1.
  APP_CGO_ENABLED: '{{.Opn}}.APP_CGO_ENABLED | default ""{{.Cls}}'

includes:
  common: ./build/Taskfile.yml
  windows: ./build/windows/Taskfile.yml
  darwin: ./build/darwin/Taskfile.yml
  linux: ./build/linux/Taskfile.yml
  ios: ./build/ios/Taskfile.yml
  android: ./build/android/Taskfile.yml

tasks:
  build:
    summary: Builds the application
    cmds:
      - task: "{{.Opn}}.GOOS{{.Cls}}:build"

  package:
    summary: Packages a production build of the application
    cmds:
      - task: "{{.Opn}}.GOOS{{.Cls}}:package"

  run:
    summary: Runs the application
    cmds:
      - task: "{{.Opn}}.GOOS{{.Cls}}:run"

  dev:
    summary: Runs the application in development mode
    cmds:
      - wails3 dev -config ./build/config.yml -port {{.Opn}}.VITE_PORT{{.Cls}}

  setup:docker:
    summary: Builds Docker image for cross-compilation (~800MB download)
    cmds:
      - task: common:setup:docker

  build:server:
    summary: Builds the application in server mode (no GUI, HTTP server only)
    cmds:
      - task: common:build:server

  run:server:
    summary: Runs the application in server mode
    cmds:
      - task: common:run:server

  build:docker:
    summary: Builds a Docker image for server mode deployment
    cmds:
      - task: common:build:docker

  run:docker:
    summary: Builds and runs the Docker image
    cmds:
      - task: common:run:docker
