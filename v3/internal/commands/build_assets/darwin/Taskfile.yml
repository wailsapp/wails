version: '3'

includes:
  common: ../Taskfile.yml

tasks:
  build:
    summary: Creates a production build of the application
    deps:
      - task: common:go:mod:tidy
      - task: common:build:frontend
        vars:
          BUILD_FLAGS:
            ref: .BUILD_FLAGS
          PRODUCTION:
            ref: .PRODUCTION
      - task: common:generate:icons
    cmds:
      - go build {{.BUILD_FLAGS}} -o {{.OUTPUT}}
    vars:
      BUILD_FLAGS: '{{if eq .PRODUCTION "true"}}-tags production -trimpath -buildvcs=false -ldflags="-w -s"{{else}}-buildvcs=false -gcflags=all="-l"{{end}}'
      DEFAULT_OUTPUT: '{{.BIN_DIR}}/{{.APP_NAME}}'
      OUTPUT: '{{ .OUTPUT | default .DEFAULT_OUTPUT }}'
    env:
      GOOS: darwin
      CGO_ENABLED: 1
      GOARCH: '{{.ARCH | default ARCH}}'
      CGO_CFLAGS: "-mmacosx-version-min=10.15"
      CGO_LDFLAGS: "-mmacosx-version-min=10.15"
      MACOSX_DEPLOYMENT_TARGET: "10.15"
      PRODUCTION: '{{.PRODUCTION | default "false"}}'

  build:universal:
    summary: Builds darwin universal binary (arm64 + amd64)
    deps:
      - task: build
        vars:
          ARCH: amd64
          OUTPUT: "{{.BIN_DIR}}/{{.APP_NAME}}-amd64"
      - task: build
        vars:
          ARCH: arm64
          OUTPUT: "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"
    cmds:
      - lipo -create -output "{{.BIN_DIR}}/{{.APP_NAME}}" "{{.BIN_DIR}}/{{.APP_NAME}}-amd64" "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"
      - rm "{{.BIN_DIR}}/{{.APP_NAME}}-amd64" "{{.BIN_DIR}}/{{.APP_NAME}}-arm64"

  package:
    summary: Packages a production build of the application into a `.app` bundle
    deps:
      - task: build
        vars:
          PRODUCTION: "true"
    cmds:
      - task: create:app:bundle

  package:universal:
    summary: Packages darwin universal binary (arm64 + amd64)
    deps:
      - task: build:universal
    cmds:
      - task: create:app:bundle


  create:app:bundle:
    summary: Creates an `.app` bundle
    cmds:
      - mkdir -p {{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/{MacOS,Resources}
      - cp build/darwin/icons.icns {{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/Resources
      - cp {{.BIN_DIR}}/{{.APP_NAME}} {{.BIN_DIR}}/{{.APP_NAME}}.app/Contents/MacOS
      - cp build/darwin/Info.plist {{.BIN_DIR}}/{{.APP_NAME}}.app/Contents
      - task: sign:app
    vars:
      SIGN_IDENTITY: '{{.SIGN_IDENTITY | default "-"}}'

  sign:app:
    summary: Sign the application bundle
    cmds:
      - |
        {{if eq .SIGN_IDENTITY "-"}}
        codesign --force --deep --sign - {{.BIN_DIR}}/{{.APP_NAME}}.app
        {{else}}
        codesign --force --deep --sign "{{.SIGN_IDENTITY}}" --options runtime {{if .ENTITLEMENTS}}--entitlements {{.ENTITLEMENTS}}{{end}} {{.BIN_DIR}}/{{.APP_NAME}}.app
        {{end}}
    vars:
      SIGN_IDENTITY: '{{.SIGN_IDENTITY | default "-"}}'
      ENTITLEMENTS: '{{.ENTITLEMENTS | default ""}}'

  sign:notarize:
    summary: Notarize the application (requires signed app with Developer ID)
    cmds:
      - wails3 sign --app {{.BIN_DIR}}/{{.APP_NAME}}.app --notarize {{if .KEYCHAIN_PROFILE}}--keychain-profile {{.KEYCHAIN_PROFILE}}{{end}}
    vars:
      KEYCHAIN_PROFILE: '{{.KEYCHAIN_PROFILE | default ""}}'

  package:signed:
    summary: Build, package, and sign with Developer ID certificate
    deps:
      - task: build
        vars:
          PRODUCTION: "true"
    cmds:
      - task: create:app:bundle
        vars:
          SIGN_IDENTITY: '{{.SIGN_IDENTITY | default "Developer ID Application"}}'
          ENTITLEMENTS: '{{.ENTITLEMENTS | default "build/darwin/entitlements.plist"}}'

  package:notarize:
    summary: Build, package, sign with Developer ID, and notarize
    deps:
      - task: package:signed
    cmds:
      - task: sign:notarize

  run:
    cmds:
      - mkdir -p {{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/{MacOS,Resources}
      - cp build/darwin/icons.icns {{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Resources
      - cp {{.BIN_DIR}}/{{.APP_NAME}} {{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/MacOS
      - cp build/darwin/Info.dev.plist {{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/Info.plist
      - codesign --force --deep --sign - {{.BIN_DIR}}/{{.APP_NAME}}.dev.app
      - '{{.BIN_DIR}}/{{.APP_NAME}}.dev.app/Contents/MacOS/{{.APP_NAME}}'
