version = 3

project {
  name = "print"
  product_name = "print"
  identifier = "com.wails.examples.print"
  version = "0.1.0"
  supported_platforms = ["darwin"]
}

frontend {
  disabled = true
}

build {
  tags = ["devtools"]
}
