version = 3

project {
  name = "server"
  product_name = "server"
  identifier = "com.wails.examples.server"
  version = "0.1.0"
}

frontend {
  disabled = true
}

build {
  tags = ["devtools", "server"]
}
