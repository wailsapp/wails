version = 3

project {
  name = "services"
  product_name = "services"
  identifier = "com.wails.examples.services"
  version = "0.1.0"
}

frontend {
  disabled = true
}

build {
  tags = ["devtools"]
}
