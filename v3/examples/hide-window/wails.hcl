version = 3

project {
  name = "hide-window"
  product_name = "hide-window"
  identifier = "com.wails.examples.hidewindow"
  version = "0.1.0"
}

frontend {
  disabled = true
}

build {
  tags = ["devtools"]
}
