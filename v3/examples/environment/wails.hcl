version = 3

project {
  name = "environment"
  product_name = "environment"
  identifier = "com.wails.examples.environment"
  version = "0.1.0"
}

frontend {
  disabled = true
}

build {
  tags = ["devtools"]
}
