version = 3

project {
  name = "streams"
  product_name = "streams"
  identifier = "com.wails.examples.streams"
  version = "0.1.0"
}

frontend {
  disabled = true
}

build {
  tags = ["devtools"]
}
