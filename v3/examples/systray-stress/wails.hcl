version = 3

project {
  name = "systray-stress"
  product_name = "systray-stress"
  identifier = "com.wails.examples.systraystress"
  version = "0.1.0"
  supported_platforms = ["windows"]
}

frontend {
  disabled = true
}

build {
  tags = ["devtools"]
}
